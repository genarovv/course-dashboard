# CHANGELOG

История изменений проекта. Правило (CLAUDE.md, «Стандарт документации» п.2): в основных документах живёт только актуальное состояние, устаревшее переносится сюда.

## 2026-08-14 (D47: логи uvicorn в едином формате #75)
- **Повод — тот же SSH-аудит journald 2026-08-14:** в журнале вперемешку два вида записей — прикладные «2026-08-13T20:35:06+0000 INFO app.services…» (контракт #75) и uvicorn'овские «INFO:     127.0.0.1:40594 - "POST /sync HTTP/1.1" 200 OK» без времени и источника. Два стиля таймстампов в одном журнале мешают читать хронологию.
- **`configure_logging` переводит логгеры uvicorn (`uvicorn`, `uvicorn.error`, `uvicorn.access`) на единый формат:** их собственные хендлеры снимаются, включается propagate к root с UtcFormatter — каждая строка получает UTC-время, уровень и источник. Момент валиден: uvicorn настраивает свои логгеры до импорта приложения, `configure_logging` выполняется при импорте `app.main`. Операция стоит до идемпотентного guard: повторный вызов снова снимает хендлеры и не дублирует root-хендлер. Текст access-сообщения самого uvicorn не переделывается — единая только обёртка «время, уровень, источник». Решение #75 «логгеры uvicorn не трогаем» отменено этим тикетом.
- Тестов 478 → 481, ruff чист. Рабочий ID D47 (тикет заводит CEO задним числом).

## 2026-08-14 (S76: журнал отвечает на вопросы аудита)
- **Повод — SSH-аудит боевого журнала:** на четыре вопроса («что происходило», «что тревожно», «сколько раз начали/довели главное действие», «чего не хватает») журнал отвечал плохо. Пробелы закрыты кодом; перенос боевой базы в `data/` и видимость cron сделаны на сервере в тот же день (см. ops/vps-setup.md).
- **События входа логируются (`app/routes/auth.py`):** успех — INFO, неудача (с номером попытки), срабатывание блокировки и вход при активной блокировке — WARNING. Всё с IP и меткой `user_marker` вместо сырого имени (контракт #75, NFR-3); неизвестное имя не пишется вовсе — в поле имени по ошибке вводят пароль. До этого вопрос «кто входил 12.08 с незнакомого IP» по журналу не решался.
- **Свод называет объём работы ядра:** при подключённом воркере — «N пар поставлено ядру» или «пар без вердикта нет». Тихий успех был неотличим от тихого отказа (класс FIX-I2: completed при нуле вердиктов).
- **httpx ограничен WARNING (`logging_config.py`):** INFO-строка на каждый запрос к хостингу давала ~90 % объёма журнала и писала полные URL репозиториев с именами студентов (ПД, NFR-3). Ошибки httpx остаются видимыми.
- **`alembic/env.py` уважает `CD_DATABASE_URL`:** перекрывается только дефолт alembic.ini; явный `set_main_option` (тестовые фикстуры) и сознательно правленый ini сильнее — иначе экспортированная переменная увела бы тестовые миграции в боевую базу. Закрывает класс инцидента 31.07 («мигрировали пустой файл») после переноса боевой базы в `data/`.
- **Потолок осмотра веток-кандидатов настраивается:** `CD_BRANCH_SCAN_LIMIT` (дефолт 5, поведение не изменилось). У С-03 кандидатов 13 — восемь не осматривались никогда; теперь слепая зона снимается переменной на сервере, без правки кода.
- Тестов 467 → 478, ruff чист. Issue #76.

## 2026-08-13 (ревью перед merge: образец переменных развёртывания)
- **Дефект в `ops/env.example` (ветка D41-vps), найден ревью и подтверждён исполнением.** Адрес базы был задан асинхронным драйвером (`sqlite+aiosqlite`), а приложение поднимает синхронный движок (`create_engine`, `app/store.py`). Отказ отложенный и оттого дорогой: движок создаётся без ошибки, служба стартует, а первый же запрос к базе падает с `MissingGreenlet`. Развернувший по инструкции читал бы это как дефект приложения. Драйвер исправлен на синхронный, к строке добавлено объяснение.

## 2026-08-13 (#75: логирование по контракту CEO)
- **Дефект наблюдаемости:** root-логгер не настраивался вовсе — прикладные `logger.info` не попадали в журнал (срабатывал только lastResort-хендлер WARNING+), а warning/error печатались голой строкой без времени, уровня и источника. Журнал VPS показывал только access-логи uvicorn и разрозненные warning.
- **Единая конфигурация** `app/logging_config.py` (подключается при старте `main.py`): каждая запись — «время UTC (ISO-8601), уровень, модуль-источник, сообщение»; идемпотентна (перезагрузка uvicorn не плодит хендлеры). `user_marker` — короткая метка оператора без ПД (8 гекс-символов sha256), в лог идёт она, а не идентификатор.
- **Главный сценарий — обход (`run_sync`) — логируется тремя INFO-записями:** «начат» (run, триггер, оператор), «завершён» (статус, число репозиториев) и «не завершился» (неожиданное исключение — контекст + трассировка, проброс наверх). Роут `/sync` передаёт метку оператора (`user_marker`) для ручного запуска; cron — без оператора (метка — сам триггер schedule).
- **Молчаливые перехваты исключений стали слышимыми:** `branch_detect.refresh_default_branch` (warning), опциональные данные без краха обхода — дата головного коммита, дата головы ветки, дата хостинга, невалидная дата `fmt_dt`, неизвестная роль в модалке, невалидный JSON ответа LLM (debug-след). Поведение не менялось — менялась только наблюдаемость (NFR-2: дата NULL — не сбой).
- **Гейт пройден:** ruff чист, 459 тестов, coverage 96,5 % (порог 90 %). Тестов 449 → 459.

## 2026-08-11 (D43: обход видит работу вне основной ветки)
- **Дефект наблюдения, найденный ручным обходом вне дашборда:** у четверых из семи студентов работа есть, а матрица показывала пустые строки. Обход читает `repo.default_branch` (ADR-006), работа лежала в других ветках. Причина системная: в форках ЦУ `main` защищена — push и merge только для роли Maintainer, студенты Developer, слить работу они физически не могут (проверено `GET /protected_branches` по четырём форкам). Инструмент честно отвечал на вопрос «что лежит в основной ветке», а его ответ читался как «что сделал студент».
- **Сделано (этап 1 — сигнал, не вердикт):** `GitClient.list_branches`; модель `BranchHint` + миграция `f5c8b2e91a37` (append-only журнал, как `MrObservation`); шаг `_scan_branches` в обходе; пометка в строке артефактной матрицы и отдельный блок в деле защиты.
- **Правила отбора:** кандидат — ветка свежее головы основной; читается её дерево, считаются роли курса (не файлы: несколько дефов одной роли — альтернативные пути одного артефакта); подсказка пишется только при приросте против основной ветки. Не более пяти кандидатов на репозиторий, урезание пишется в лог поимённо. Ошибка шага — предупреждение, а не исход репозитория (NFR-2): подсказка это удобство, портить наблюдение артефактов она не вправе.
- **Канон вердиктов не тронут:** снапшоты и связность по-прежнему только по основной ветке, свод честно отвечает «в основной ветке пусто». Вердикты по веткам-кандидатам не считаются — это этап 2 и отдельный ADR (что считать каноном идентичности вердикта, когда пара артефактов существует в двух ветках).
- **Живой прогон на репозиториях потока поймал бесполезность первой редакции меры.** Считалось «сколько ролей курса найдено по контрактному пути» — и ветка с 37 файлами работы давала ровно тот же счёт, что пустой шаблон: студент, у которого работа в чужой ветке, почти всегда и путей контракта не соблюдает. Мера пересобрана: роль засчитывается и по контрактному пути, и по кандидату «не там» (D44 в стеке под этим MR). После правки подсказки появились там, где и должны были.
- **Заодно закрыт пробел контракта артефактов:** в роли `code` не было ни одного паттерна под нативные стеки — приложение на Swift не засчитывалось как код ни в одной ветке. Добавлены `**/*.swift` и `**/*.kt`.
- Тестов 424 → 444 (вместе с D44 в стеке), ruff чист. Issue #69.
## 2026-08-11 (D44: заготовка по контрактному пути не маскирует настоящий файл)
- **Дефект (боевой, найден ручным обходом вне дашборда):** у одного репозитория потока обход давал `found=0` при восьми `template_copy` — при том, что работа в репозитории есть и прочитана глазами. Причина: `_find_displaced` вызывался только в ветке «по контрактному пути не найдено ничего». Если там лежала шаблонная заготовка из форка, срабатывала ветка `template_copy`, и настоящий файл уровнем ниже (`d.kolesov/ARCHITECTURE.md` при заготовке в корне) не искался никогда. Заготовка вытесняла реальную работу — хуже, чем «не нашли»: преподаватель читает пустую строку как «студент не сделал ничего».
- **Сделано:** при заготовке или пустом файле по контрактному пути обход ищет кандидата «не там» и, если тот не является заготовкой, наблюдает его — `partial`, причина `wrong_place`, `file_path` настоящего файла. Контракт путей (ADR-003) не ослаблен: «не там» по-прежнему не равно «сдано».
- **Заодно два пробела того же класса:** сопоставление имени стало регистронезависимым (`ARCHITECTURE.md` против `architecture/architecture.md`); для паттернов с глобом (`product/user-stories/*.md`) поиск «не там» включён с якорем по имени последнего каталога — раньше он был выключен целиком, чтобы `*.md` не выродился в «любой md где угодно».
- **Границы стоимости и шума:** читается не более трёх кандидатов на артефакт (`_MAX_CANDIDATES_READ`); кандидаты из `archive/`, `node_modules/`, `build/`, `dist/`, `vendor/`, `.venv/`, `__pycache__/`, `.next/` отбрасываются. `target/` намеренно не в списке шумных — у одного из репозиториев потока это рабочий каталог проекта. Выбор кандидата детерминирован: ближайший к корню, затем по алфавиту.
- Тестов 424 → 433, ruff чист. Issue #70.
## 2026-08-11 (D44, добор: контрактный путь под лишним префиксом)
- **Недоделанный критерий приёмки #70.** В AC записано «кандидатом считается контрактный путь с лишним префиксным сегментом», реализованы были только якоря по имени файла и по имени последнего каталога. Для паттернов вида `src/**/*.py` оба бессильны: предпоследний сегмент — `**`, привязаться не к чему, и поиск сдавался целиком, чтобы не выродиться в «любой py-файл где угодно».
- **Цена на живом репозитории:** у студента С-02 роль «код» показывалась пустой при 32 файлах кода — весь код лежит в `target/src/API/…`, а `src/**/*.py` под префиксом не совпадает. Замечено CEO при разборе матрицы 2026-08-11.
- **Сделано:** добавлен третий якорь — вся структура контрактного пути под произвольным префиксом (`^(?:[^/]+/)+` + паттерн). Правило узкое: структура обязана совпасть целиком, а не хвостом имени. Кандидаты трёх якорей объединяются без дублей, порядок прежний — ближе к корню, затем по алфавиту; шумные каталоги отбрасываются и под префиксом.
- **Проверено живым прогоном 2026-08-11 на семи репозиториях:** С-02 — ролей 8 → 10, С-04 — 8 → 9; подсказки по веткам у С-05 (`dev`, 7 против 4) и С-07 (`sleepy-code`, 5 против 4) не пострадали, новых ложных срабатываний нет.
- Тестов 449 → 456, ruff чист. Issue #70.

## 2026-08-11 (D45: реестр в CSV — источник правды в обе стороны)
- **Пробел, вскрытый живой задачей:** `archived_at` описан в `data-model.md` как штатный механизм («ошибочный URL архивируется, не удаляется»), но пути записи не существовало — в `store.py` только чтение, в `routes/admin.py` только импорт, обход и перезагрузка конфига. Двое из реестра не проходят курс (решение CEO 2026-08-11), и убрать их из матрицы было нечем. Заодно выяснилось, что прежнее «исключение С-10 и С-11» механизмом не пользовалось: их просто ни разу не вносили в CSV.
- **Сделано:** импорт делает CSV источником правды в обе стороны — репозиторий, которого в файле нет, архивируется; вернувшийся в файл разархивируется. Снапшоты и вердикты сохраняются всегда (FR-9), удаления нет. Добавлены две узкие мутации `store.archive_repository` / `store.restore_repository` (контракт §3.5: мутации только через store).
- **Защита от случайного обнуления:** файл, в котором не распознан ни один адрес, отклоняется с 400 — «архивировать всё» не должно быть достижимо одним нажатием. Каждая архивация и разархивация пишется в лог поимённо: исчезновение строки с экрана обязано быть объяснимо.
- **tests-change** в `tests/test_csv_import.py`: два утверждения на точное равенство сводки дополнены полями `archived` и `restored`. Это настоящее изменение контракта ответа импорта, а не правка ради зелёного прогона — сводка теперь сообщает результат реконсиляции. Смысл прежних проверок (доступные, недоступные, дубликаты) не тронут.
- Тестов 424 → 429, ruff чист. Issue #71.

## 2026-08-04 (D42: кнопка «обновить сейчас» гаснет на время обхода)
- **Решение CEO** по боевому 500: повторное нажатие во время идущего обхода давало `database is locked` (транзакция обхода держит единственного писателя SQLite). Кнопка гасла только во вкладке, где её нажали, — перезагрузка и вторая вкладка возвращали её активной.
- **Сделано:** `is_sync_running` — серверная истина по `SyncRun.status` (обе матрицы получают `sync_running`, кнопка рендерится погашенной с подписью «обход идёт…»); `POST /sync` при идущем обходе отвечает 409 «обход уже идёт», а не падает — эта проверка ловит и вторую вкладку, и cron. Обход, висящий `in_progress` дольше 5 минут (порог выбран CEO при обходе ~1,5 мин), считается следом упавшего процесса — иначе кнопка залипла бы навсегда: `ASSUME_DEAD_AFTER`.
- **tests-change** в `tests/test_polish_d22.py`: фикстура `_seed` теперь закрывает обход статусом `completed`. Утверждения AC 5 не менялись — в фикстуре обход оставался `in_progress`, и после D42 тест проверял бы состояние «идёт обход», которого он не имел в виду. Тестов 416 → 424.

## 2026-08-04 (FIX-I2: обход коммитит наблюдения до свода)
- **Дефект (боевой, найден первым обходом после недельного перерыва):** обход `731b4991` завершился со статусом `completed`, записал 4 новых снапшота — и не посчитал ни одного вердикта; в логе «И2: снапшоты пары не найдены» на каждую пару. `run_sync` делал `flush()`, а коммит происходил позже, в зависимости `get_session`, — воркеры ядра FR-5 открывают собственные сессии (`make_verdict_worker`) и незакоммиченных снапшотов не видели. Класс: тихий отказ (система рапортует успех, работа не сделана).
- **Почему дожил до боя:** проявляется только при изменившемся артефакте — при неизменном хеше D28 не пишет новый снапшот, и пара строится на давно закоммиченном. Прогоны 30–31.07 считали вердикты именно на таких. Тесты воркера получали готовые закоммиченные фикстуры.
- **Фикс:** `session.commit()` перед сводом-реконсиляцией (`sync_orchestrator.run_sync`), ARCHITECTURE §5.1 — шаг внесён в схему обхода. Репро-тест `tests/test_sync_commit_before_reconcile.py` (красная фаза в истории): чужая сессия видит снапшоты обхода; вердикт считается в том же обходе, в котором артефакт впервые наблюдён. Тестов 414 → 416.
- **Не входит в этот MR:** `database is locked` при повторном POST /sync во время идущего обхода — отдельный вопрос (транзакция обхода остаётся длинной), вынесен CEO как отдельный тикет.

## 2026-07-31 (D38/D39/D40: пятая итерация закрыта)
- **D38 (P1):** легенда объясняет сводку строки (⚠ N / X/M / «мимо ревью»); активная сортировка помечена, дефолт — «по реестру ▼»; сущность в чипе усечена по границе слова; кнопка курирования — глагол «пометить ложным» + подпись «отметка обратима» во всех трёх местах; заголовки колонок с подсказками-расшифровками (ROLE_HINTS).
- **D39 (P2):** заголовок модалки «Роль — имя репо»; «ожидался, не найден» вместо «не наблюдался»; равные хеши сторон — один @sha; легенда на матрице занятий; статусы MR по-русски (открыт/влит/закрыт, MR_STATE_LABELS); «сдаётся через merge request — файлов в основной ветке нет» вместо двойного отрицания; role_title в таблице разрывов матрицы занятий (пропуск D37).
- **D40 (дизайн-проход, скилл frontend-design по вызову CEO):** токены «бумага/чернила/сургуч/грифель/архивный синий»; Georgia для h1/h2 (тон лабораторного журнала); моноширинный для доказательных данных (@sha, даты, счётчики); сигнатура — «титульный лист дела» в режиме защиты (двойная рамка + резюме-штамп). Статусные подложки не тронуты (решение CEO 2026-07-30). Тестов 400 → 411. README §Статус актуализирован, план §7.

## 2026-07-31 (C5: принудительный пересчёт вердиктов на русский)
- **C5 (решение CEO №2):** версии всех 9 рубрик рёбер в config.yaml подняты на +0.1 (тексты не менялись) — после reload-config у пар не будет валидных вердиктов на новые четвёрки, следующий обход пересчитает всё русским промптом C4; старые вердикты остаются в журнале (append-only). **Гейт пройден: golden set 2/2** (запись в golden-set.md); R-1/R-2 без изменений, R-3 — известная нестабильная пара, мигнула ok→break (кандидат на эталонирование, ручная сверка преподавателем).

## 2026-07-31 (D24/D36/D37: пятая итерация, продолжение)
- **D24:** хронология защиты — пара «исчез»+«появился» одной роли в одном обходе схлопнута в «перемещён: старый → новый путь» (флаппинг переноса файлов больше не выглядит сбоем); MR-события с датами хостинга встроены в общую ленту с пометкой «дата хостинга» (независимые даты против «я сдавал раньше»); MR без даты — только в свёрнутой таблице.
- **D36 (решения CEO №3, №4):** шапка-резюме дела («сдано X/M · разрывов: N · MR: K, мимо ревью: J · работа ДД.ММ — ДД.ММ», период по датам хостинга, фолбэк — даты коммитов/наблюдений); «погашенных разрывов: 0» показывается явно (негативный AC D19 отменён решением CEO); `GET /defense` — нейтральный список дел для проектора (без статусов и сравнений) + ссылка из матрицы. `merged_no_review_count` и `repo_progress` переехали в evidence_chain (один факт в одном месте).
- **D37:** русские имена ролей во всех экранах — ROLE_TITLES/`role_title` в labels.py, дело защиты/карточка/модалка больше не показывают слаги (data_model → «Схема данных»); слаги остаются в якорях и CSS-классах (контракт D17). Тестов 388 → 400.

## 2026-07-31 (D23: чистка дублей artifact_def — пятая итерация)
- **D23 (спека `plans/доводки-ux-5-2026-07-31.md`, решения CEO 2026-07-31):** смена ключа реконсиляции в пакете «12 артефактов» оставила по два одинаковых определения у 7 ролей боевой БД (verified фокус-группами: дубль файла в модалке, лишние LLM-пары). Чистка — **версионированной миграцией `e9d23a5c1f04`** (механизм выбран CEO явно): вердикты перепривязываются на эквивалентный снапшот хранимого определения (тот же content_hash — четвёрка Б1 не меняется), снапшоты дубля и дубль удаляются; ровно два append-only триггера И5 снимаются на время миграции и восстанавливаются безусловно; группа без эквивалента пропускается (fail-safe). Runtime-guard в `reconcile` триггеры НЕ трогает: удаляет только дубли без наблюдений (свежий артефакт бага), дубли со снапшотами — warning; счётчик `artifact_defs_deduped` в сводке. Тестов 383 → 388.

## 2026-07-31 (доработка по ревью итерации 4)
- Ревью `reviews/ревью-итерация-4-2026-07-31.md`: APPROVE по всем пяти тикетам, 0 блокеров, 1 серьёзное + 8 замечаний. Закрыто этим коммитом: хронология защиты показывается и сортируется в местном времени (#32 — иначе даты съезжали на день рядом с местными вехами занятий); `"committer": null` от GitHub → None, а не AttributeError (обход жив); правило «мимо ревью» — одна функция `merged_no_review_count` (один факт в одном месте); несмежные пустые MR-занятия подписываются перечислением («занятия 11, 13»), а не ложным диапазоном; тест стабильности sort=lag получил пару с равной долей; негативный сценарий D19 «все снапшоты одного коммита» покрыт тестом; дата MR в карточке студента — через `fmt_dt` (tests-change); кнопка «обновить сейчас» перезагружает страницу и при сетевой ошибке (не зависает в «обход идёт…»). Тестов 379 → 383.

## 2026-07-31 (D22: полировка — четвёртая итерация закрыта)
- **D22 (#68):** легенда без жаргона («не наблюдался» → «нет данных»; «сдача через MR» → «сдача через merge request (запрос на слияние)»); переносы в колонке репо по границам слов (overflow-wrap); URL в шапках модалки и карточки студента заменён ссылкой «открыть репозиторий» (полный адрес в title); «защита» → «защита проекта»; кнопка «обновить сейчас» на стикере актуальности обеих матриц (POST /sync с подтверждением, кнопка блокируется на время обхода); пустые MR-колонки матрицы занятий (все ячейки «сдача через MR») схлопнуты в одну «занятия N–M: сдача через MR» (одна такая колонка не схлопывается); таймстемпы интерфейса без микросекунд (хронология карточки — fmt_dt). Тестов 369 → 379.

## 2026-07-31 (D21: честная ячейка и видимое курирование)
- **D21 (#67):** ячейка «есть» с активным разрывом говорит «сдано, но разрыв» (конфликт «зелёное + бордовое» снят); чип разрыва обрезается CSS line-clamp до 2 строк, сущность в чипе усечена до ~80 символов (полный текст — в модалке); кнопка «ложный разрыв» в модалке — видимая кнопка-действие в блоке разрыва с подписью «отметка обратима» (была текстовой строкой в подвале карточки ребра). Свод «частично» не тронут (спека адресует только «есть»). Тестов 363 → 369.

## 2026-07-31 (D20: сводная колонка строки + две сортировки)
- **D20 (#66):** сводка строки в закреплённой зоне у имени репо — «⚠ N» (уникальные разрывы, счётчик D15 переехал сюда), «X/M» (сдано ролей из ожидаемых файлами; found+partial = сдано по BR-3; роли «сдача через MR» вне знаменателя), шильдик «мимо ревью: K» при K>0 (сигнал процесса FR-12). В шапке две сортировки: «по разрывам» (`?sort=breaks`, D15) и «по отставанию» (`?sort=lag`, по возрастанию X/M; пустая строка первой), обе стабильны к реестру; подпись «сначала проблемные» ушла. Правая колонка «Разрывы» удалена (один факт — в одном месте); контрактные проверки D15 перенесены на сводку с пометкой tests-change. Тестов 355 → 363.

## 2026-07-31 (D19: дело защиты v2)
- **D19 (#65):** `source_commit_date` в снапшоте (миграция c7d19a4e5b02; сбор в обходе через `git_client.get_head_commit_date`, любой сбой → NULL — не крах, NFR-2). Режим защиты v2: шапка «Защита: <короткое имя>» + полный URL мелко + дата открытия; notes вердикта — основной текст карточки разрыва, файловая детализация точек — по раскрытию; хронология схлопнута до переходов состояний (появился / статус изменился / исчез; поток по artifact_def — альтернативные пути роли не смешиваются) с вехами «занятие N» из `lesson.date`; дата перехода — дата коммита, без неё — дата наблюдения с пометкой «зафиксировано обходом»; полный лог — по раскрытию; MR-история и погашенные разрывы («снят преподавателем ДД.ММ») — свёрнутыми, без кнопок FR-10 (контракт D18 не ослаблен). Время в режиме — ДД.ММ ЧЧ:ММ без микросекунд (`timeutil.fmt_dt`, глобал Jinja вместе с `repo_short_name`). Тестов 338 → 355.

## 2026-07-31 (C4: русский выход ядра — четвёртая итерация, UX-доводки)
- **C4 (#64, решение CEO по UX-фокус-группам 2026-07-31):** промпт ядра требует русский язык для свободнотекстовых полей выхода (`notes`, `why` в точках); названия сущностей и цитаты — на языке документа студента, цитата дословная, не переводится. Валидатор не изменён (язык — инструкция промпта, не схема; английский ответ — приемлемая деградация). Гейт пройден: перепрогон golden set 2/2 (записи в `evals/golden-set.md`, `llm_model: deepseek-v4-flash`), реальные пары R-1…R-4 — вердикты совпали с прогоном 30.07, свободный текст пришёл по-русски. Старые англоязычные вердикты в БД не трогаются (журнал append-only) — уйдут естественным пересчётом четвёрок. PRD не меняется (качество реализации FR-5).

## 2026-07-31 (FR-11-пилот)
- #45: скрипт `evals/fr11_pilot.py` (вне продукта, решения CEO 2026-07-09/28) — рубрика качества README v1.0 (5 критериев: назначение, запуск, навигация, статус, честность; драфт агента, на утверждении CEO), прогон по последним readme-снапшотам боевой БД, строгий JSON, модель зафиксирована (канон Б1). Отчёт локальный (в git не попадает — цитаты студентов), формат «ручная сверка преподавателя = замер точности» для решения о FR-11 в v2. Тестов +8 (абсолютные числа сместила параллельная третья итерация — см. прогоны ревью).
## 2026-07-31 (D15–D18: P1 + режим защиты — третья итерация закрыта)
- **D15 (#59):** колонка «Разрывы» — счётчик уникальных рёбер-разрывов на строку (дубль источник/приёмник не удваивает перепись); сортировка «сначала проблемные» `?sort=breaks` (стабильная, живёт в URL — переживает POST отметок FR-10); закреплённые шапка и колонка репозитория.
- **D16 (#60):** метки свежести — «новое» на чипе, чей вердикт впервые вычислен в последнем обходе (по `computed_at`; перепрогон той же четвёрки не «мигает», D25), точка на ячейке изменившегося артефакта; метки существуют только при наличии предыдущего обхода. Store: `find_previous_sync_run`; карточка ребра несёт `computed_at`.
- **D17 (#61):** уверенность в модалке — бейдж по уровню (не мелкий серый текст); ссылка «открыть в карточке →» с якорем на ребро (+`id` на рёбрах карточки); фокус переносится в модалку, Tab-ловушка, Esc возвращает фокус ячейке.
- **D18 (#62), FR-13/US-C2 (утверждена CEO):** режим защиты `GET /students/{id}/defense` — полноэкранное дело одного студента: только разрывы «высокой» уверенности и не погашенные (митигация риска §11 реализована), даты вычисления и коммиты обеих сторон у каждого вердикта, связные рёбра, хронология; кнопок FR-10 нет; входы со строки матрицы и из карточки; слепая зона — пометка. PRD → v2.7 (FR-13, риск §11 → реализовано), US-C2 внесена в канон историй.

## 2026-07-30 (D6: deferred в UI + объединение связок)
- #37: карточка различает «проверяется» / «отложено: LLM недоступна» / «отложено: ответ не распарсился» (state deferred + причина, рендер в student_card). Копилка ревью ядра/R/T2: LLM-клиент создаётся на пару и закрывается воркером (фабрика вместо разделяемого экземпляра — некому было закрыть); валидатор отклоняет точки без entity и не-объекты; probe_findings без json-null (SQL NULL как у partial_reason); MULTIFILE_HEADER без мета-комментария; displaced-файлы проверяются пробами (тест).
- Решение CEO №2 по связкам: список путей мультифайловой стороны = ОБЪЕДИНЕНИЕ всех глоб-связок роли (lib+functions+src — одна кодовая база; отдельные связки «теряли» задачи другой половины). Рубрики plan→code и code→tests подняты до v1.2 — шумные вердикты v1.1 замещаются пересчётом четвёрки. Тестов 265 → 275.

## 2026-07-30 (R/T2: доработка по ревью + решения CEO)
- Мультифайловая сторона пары ядра = список путей связки (решение CEO): `_side_text` в coherence_analyzer (дерево ревизии — раз на пару), рубрики plan→code и code→tests подняты до v1.1 с явной строкой про списки — ложные break v1.0 по «представителю» уйдут пересчётом (смена рубрики = смена четвёрки). Кросс-произведение альтернатив роли сохранено (решение CEO).
- Док-блокеры ревью: data-model §1.4/§1.9 (content_probes/probe_findings), golden-set.md — раздел рёбер с боевыми кандидатами и вейвером CEO на «пару до включения», MAP.md (+test_edges_8, +test_content_probes). ProbeConfig: валидация «ровно одно из contains/not_contains» (fail-fast конфига) + проба «Стандарт отладки» (з.13, из тикета #44). По аудиту D7: ARCHITECTURE §3.1 дополнен artifact_matrix.py и GET /artifacts. Тестов 262 → 265.

## 2026-07-30 (T2: пробы содержимого)
- #44: content_probes узкой редакцией (решение CEO 2026-07-28, развилка 4) — regex-пробы contains/not_contains только к объявленным требованиям (CLAUDE.md: «Стандарт документации» з.10, «Стандарт тестирования» з.11, маркер «Стек: <»; ARCHITECTURE: шаблонная секция «Уточняющие вопросы»). Findings пишутся в снапшот (миграция b3f1a92c7d10 с downgrade), входят в наблюдение D28, статус ячейки НЕ меняют (BR-3 нетронут); признак — блок «Пробы содержимого» в карточке студента. Мульти-файловые наблюдения пробами не проверяются (нет цельного контента). Битый regex пробы — warning, обход жив (NFR-2). Тестов 256 → 262.

## 2026-07-30 (R: рёбра связности 2→9)
- #42: конвейер курса целиком — 6 новых рёбер с рубриками v1.0 (jtbd→US, персоны→US, US→prd, архитектура→план, план→код, код→тесты) к трём существующим. Специфика в рубриках: story без работы — не разрыв (вопрос качества, не связности); «не начатые» задачи плана не судятся; служебные файлы кода не считаются значимыми. Тексты утверждены решением CEO 2026-07-30. Тестов 252 → 256.
## 2026-07-30 (D14: артефактная матрица — главный экран; PRD v2.6)
- **Свап видов (#58, решение CEO по фокус-группам):** `GET /` — матрица «репозиторий × артефакт» (алиас `/artifacts` сохранён), матрица занятий переехала на `GET /lessons`; взаимные ссылки развёрнуты. Контрактные тесты матрицы занятий переведены на `/lessons` с пометкой tests-change (проверки содержимого не ослаблены).
- **PRD → v2.6:** FR-4 — «Матрица наличия артефактов», представление в двух видах (главный артефактный, вспомогательный по занятиям), критерии статусов общие; §2 обновлён. US-B1 — уточнение о главном виде (канон видов — PRD).
- Попутно обнаружено и исправлено: ветка `docs-2026-07-23` (PRD v2.5, NFR-3/R-PD2; закрытия реестра от 23.07) числилась в MEMORY смерженной, но в origin/main не попала — влита в этот стек, реестр сверен.

## 2026-07-30 (D9–D13: доводка артефактной матрицы, P0 третьей итерации)
- Основание: решения CEO по фокус-группам (спеки — `plans/доводки-артефактной-матрицы-2026-07-30.md`, тикеты #53–#57). Каждая задача — tests first.
- **D9 (#53):** роль MR-занятия без найденного артефакта — плашка «сдача через MR» (класс `acell-mr`), не красное «нет»; правило двухуровневое (best-wins → канал); объяснение в модалке. Закрывает регрессию US-B7 в артефактном виде.
- **D10 (#54):** слепая зона, хроники и auth-баннер — теперь и в `/artifacts` (функция `blind_spots_and_signals` стала общей в matrix_builder); стикер «Актуально на» краснеет при обходе старше 48 часов (US-A3: не тихо устаревшие данные).
- **D11 (#55):** честный свод ячейки — разрыв называется и при «частично»; погашенный разрыв показан «помечен ложным», а не «связность ок» (BR-2: решение человека видно); приоритет свода: разрыв > проверяется > помечен ложным > ок.
- **D13 (#57):** инверсия сигнала (решение CEO, осознанное отступление от сплошных заливок макета) — статусы на тихих светлых подложках, единственный яркий элемент — чип разрыва «⚠ потеряна „X‟»; уверенность видна до клика: высокая — полный чип, средняя/низкая — контурный с подписью словами (будничная часть митигации риска PRD §11; полная фильтрация — режим защиты, #62).
- **D12 (#56):** легенда — все состояния ячейки словами, обе градации чипа, правило «частично» (BR-3) по наведению.
- Тестов 256 → 280. Впереди: D14 (#58) — свап главного экрана + PRD v2.6 + контрактные тесты D1.

## 2026-07-30 (D7: матрица «репозиторий × артефакт» по макету CEO)
- Новый вид `GET /artifacts` (в дополнение к матрице занятий FR-4, не вместо неё): строки — репозитории, колонки — роли артефактов из конфига в порядке первого занятия; ячейка цветокодирована (зелёный «есть» / оранжевый «частично» / красный «нет» / серый «не наблюдался») и содержит усечённый результат анализа: причины «частично» по-русски, для найденных — свод вердиктов рёбер FR-5 с первой потерянной сущностью («есть · разрыв: потеряна „X‟»); найденный артефакт в неснятом разрыве дополнительно обведён.
- Клик по ячейке (HTMX) — модальное окно деталей: найденные файлы роли (путь, статус, commit), связи с другими артефактами (рёбра с вердиктами, точками-цитатами и заметками преподавателю), кнопка «ложный разрыв» (FR-10). Отметка override гасит разрыв и в своде ячейки.
- Сервис `services/artifact_matrix.py`, шаблоны `artifact_matrix.html` + `artifact_cell_modal.html`, HTMX подключён в base.html (CDN, кандидат на vendoring), стили — в `static/css/app.css` (заодно мягкая подсветка статусов в старой матрице и хронологии карточки). Взаимные ссылки между двумя матрицами. Тестов 238 → 252.
- Границы, согласованные с PRD: пер-артефактный LLM-анализ в ячейке не строим — это FR-11 (v2); в ячейке — наличие (FR-4) + свод рёбер (FR-5), где роль участвует.

## 2026-07-30 (пакет «12 артефактов»: конфиг покрывает описание курса)
- Решение CEO по ручному аудиту лучшей репы (10 из 12 артефактов есть — конфиг видел 1): роли +6 (`jtbd`, `readme`, `changelog`, `adr`, `claude_md`, `roles_roster` — enum строкой, без миграции), альтернативные пути на роль (REQUIREMENTS.md=prd, DATA_MODEL.md/DATABASE.md=схема, plan.md/TASKS.md=план, USER_JOURNEY.md=US, HYPOTHESES.md/DISCOVERY.md=исследования), мультистековые code/tests (Dart/JS/src, test/), занятия 2 (контекст) и 10 (документация) в конфиге.
- Новое ребро **prd → architecture** (рубрика v1.0) — единственное, у которого обе стороны уже лежат в реальных репо: даёт живые вердикты до сдачи схем данных. Артефакты 11 (URL прототипа) и 12 (доработанные сценарии) — сознательно вне обходчика (проверка преподавателем). Тестов 228 → 234.
- Fix (найден первым же боевым прогоном): реконсиляция хранила один паттерн на (занятие, роль) — альтернативные пути затирали друг друга, конфиг «подгонялся» под последнего студента. Ключ дефа теперь (занятие, роль, паттерн); агрегация ячейки двухуровневая — внутри роли альтернативы по лучшему статусу, между ролями прежнее строгое правило D1. Исторические снапшоты остаются на перепрофилированных дефах (append-only, file_path хранит правду). Тестов 234 → 237.
- Fix по ревью T3: карточка студента выбирает альтернативу роли по лучшему статусу, а не по свежести (found важнее свежей заготовки шаблона — устранён ложный break на защите); data-model §1.4/§2 — 15 ролей и ключ (занятие, роль, паттерн); `src/**/*` сужен до кодовых расширений (py/ts/tsx/js); MAP.md актуализирован. Тестов 237 → 238.

## 2026-07-30 (C1/C2: доработка по ревью ядра)
- Блокер C2: канон ключей `points` — **{entity, quote, why}** (решение CEO): промпт клиента, data-model §1.10 и рендер отчёта мини-эвала приведены к ключам, которые уже читают карточка студента и evidence_chain (три несовместимых схемы ликвидированы). Обязательный перепрогон golden set после смены промпта: **2/2 совпадения** (закрывает и AC C2 «прогон golden set»); пограничная R-3 сменила вердикт ok→break — кандидат на эталонирование (см. golden-set.md).
- Блокер C1: восстановлены контрактные тесты валидатора, утерянные при переносе из мини-эвала (отрицательные/bool счётчики, points-не-список, домен confidence).
- Средние: модель четвёрки передаётся клиенту явно (`check_coherence(model=pair.llm_model)` — вердикт не припишется чужой модели, тест на проброс); И2-нарушение в воркере логируется отдельно как инвариант (тест на путь); докстринг Lock честен («сериализует обработку пары целиком»); git-кейс внесён в псевдокод ARCHITECTURE §5.2; INDEX.md — ADR-004 Accepted. Тестов 223 → 228.

## 2026-07-30 (C2: coherence_analyzer — ядро подключено)
- #36: `services/coherence_analyzer.py` — `ensure_verdict` по §5.2: D25 «не мигаем» (deferred пересчитывается), И2 кодом (репозиторий и роли соответствуют ребру, нарушение = ValueError без записи), тексты из git по `file_path @ source_commit_sha` (контент не храним — С4), ok/break с полями контракта, деградации parse_error/llm_unavailable → deferred; git-сбой — без записи, пара вернётся следующим сводом. `make_verdict_worker`: собственная сессия на пару, записи сериализованы Lock (единый writer SQLite).
- Проводка: `POST /sync` собирает воркер из `get_llm_client` (без ключа — None, пары честно «проверяется»). Store: +4 точечных `find_*` (snapshot/rubric/edge_def/artifact_def по id). Доки: ARCHITECTURE §3.1/«Пометки v3», CLAUDE.md (железное правило исполнено), README §Статус, MAP.md. Тестов 210 → 223.

## 2026-07-30 (C1: llm_client — первый компонент ядра)
- #35: `app/clients/llm_client.py` — `check_coherence` (DeepSeek, temperature 0, json_object, ключ/адрес/модель из settings) + `validate_llm_response` (schema-check §5.2, нормализация регистра, целостность счётчиков, ≤5 точек); 1 ретрай → None (deferred parse_error у вызывающего), HTTP/сеть → `LLMUnavailableError`. Промпт-каркас v1 канонически переехал сюда из мини-эвала.
- AC «golden set прогоняется через клиента»: `evals/mini_eval.py` переведён на `LLMClient.check_coherence` (дубли промпта/валидации удалены); контрольный прогон — вердикты идентичны прямому (GS 2/2, R-1..R-3 ok, R-4 break). Тестов: +11 клиента, −13 дублей мини-эвала (сьюта 212 → 210).

## 2026-07-30 (C3: мини-эвал — гейт Фазы 0 снят)
- #34: скрипт мини-эвала `evals/mini_eval.py` (вне продукта): 6 пар — GS-1/GS-2 с эталонами + 4 реальные (С-01: REQUIREMENTS→DATA_MODEL, DATA_MODEL→ARCHITECTURE; С-02: jtbd→prd, prd→ARCHITECTURE); промпт по каркасу v1, валидация по контракту §5.2, `llm_model` фиксируется (канон Б1). Маппинг репо и кеш текстов студентов — вне git (`evals/pairs/`). Тестов 178 → 193 (+5 по ревью).
- **Прогон 2026-07-30: golden set 2/2, реальные пары 3 ok + 1 честный break. Решение CEO: ADR-004 Accepted, гейт Фазы 0 снят.** Алиасы `DEEPSEEK_API_KEY`/`DEEPSEEK_API_BASE_URL` без префикса CD_ поддержаны, base_url посредника уважается.
- Пост-фактум ревью эфирного коммита ADR-006 (14e68fd): APPROVE с замечаниями — `reviews/ревью-пост-фактум-ADR-006-2026-07-30.md`; находки ушли в тикеты #48–#51.

## 2026-07-30 (H1: хуки и гейты к занятию 14)
- #52: версионируемые git-хуки в `hooks/` (активация `git config core.hooksPath hooks`): `commit-msg` — защита существующих тестов (правка/удаление без «tests-change: …» блокируется, стандарт тестирования п.3 стал машинным гейтом), `pre-push` — ruff + полная suite с coverage-гейтом (`fail_under = 90`, pytest-cov в dev-зависимостях). README §Хуки и гейты (включая честные границы: `--no-verify`, файловая грубость гейта, порог только при `--cov`). Тестов +6 (tmp-репо, subprocess).
- Fix по ревью H1: хукам выставлен execute-бит (без него git на Linux/macOS молча их игнорирует), no-op `omit __pycache__` убран, MAP.md актуализирован (178 → 184). Связка: pre-push проходим только вместе с фиксом протухшего теста (#49, ветка fix49-test-rot) — мержить H1 после/вместе с ним.

## 2026-07-30 (#48: пустой проект не роняет обход; #50: контракт store)
- #48: `default_branch: null` у пустого GitLab-проекта больше не валит обход и импорт (NFR-2) — None не пишется в NOT NULL, репозиторий честно уходит в repo_unavailable; найдено пост-фактум ревью ADR-006. Тестов +2.
- #50: мутация `repo.default_branch` узаконена — `store.update_repository_default_branch` (контракт §3.5: 4 → 5 узких `update_*`), дублировавшийся детект-блок собран в `services/branch_detect.py`, прямые присваивания в сервисах запрещены тестом-ограничителем (по образцу S4). ARCHITECTURE §3.2/§3.5 и ADR-006 дополнены; снята ложная ссылка эфирного коммита на «(#6)». Тестов +2.

## 2026-07-30 (#51: slash-ref)
- #51: `get_head_sha` для GitLab кодирует ref в путевом сегменте (`quote(ref, safe='')`) — ветки вида `feat/x` больше не дают ложный 404 (найдено мини-эвалом C3 на реальной ветке студента). Аудит остальных мест: ref в query-параметрах слэш допускает, GitHub `commits/{ref}` слэш переносит (закреплено тестом). Тестов +2.

## 2026-07-29 (ночь: FR-12 реализован)
- Смена порядка сдачи (решение CEO): студенты мержат сами — вердикт читается и у merged, «смержен мимо ревью» стал видимым сигналом, «кнопка преподавателя» ушла из UI; вердикт наследуется при неизменной голове (экономия API). Тестов 174 → 178.
- #38–#41: наблюдение канала сдачи MR целиком — MR-эндпоинты клиента (NoteInfo с временем), журнал MrObservation (миграция 9e51c07d2af4, И12), MR-шаг обхода (маркеры недель из config.yaml, вердикт «принято» с правилом устаревания после force-push — ADR-007 сц. 5, дедуп пагинации, деградация NFR-2), MR-блок в карточке (ready_for_merge, даты, «не найден» у маркеров) и колонка «Процесс» в матрице. Тестов 147 → 174.
- Тикеты второй итерации заведены: #34–#46. since_lesson у маркеров отложен (нет связи MR↔занятие в v1).

## 2026-07-28 (ночь: вторая итерация открыта)
- Совещание «приоритизация проверки заданий»: 6 решений CEO (протокол в decisions/meetings/). План второй итерации — plan.md §4 (4 волны).
- W-док FR-12: US-B7, FR-12 в PRD, драйвер D40, ADR-007 (наблюдение канала сдачи MR, негативные сценарии), MrObservation §1.14 + И12 в data-model.
- FR-12 интерим: `lesson.submission_channel` (миграция 7c2a91e40b15 с downgrade), конфиг-реконсиляция канала, ячейки занятий 11–13 показывают «сдача через MR, не наблюдается» вместо ложного «нет».

## 2026-07-28 (вечер, после эфира занятия 13)
- Живой баг эфира: детект default-ветки при импорте и обходе (ADR-006) — репозиторий с кодом в master больше не попадает в слепую зону.
- #33: Settings не падает от лишней переменной в .env (extra="ignore").
- #32: «Актуально на» показывает местное время с меткой зоны (CD_TZ_OFFSET_MINUTES, по умолчанию зона сервера); контракт формата as_of заменён: «ЧЧ:ММ» → «ЧЧ:ММ (зона)»; datetime.utcnow по всему проекту заменён на app/timeutil.utcnow (предупреждений 657 → 135).
- #31: обход пустого реестра больше не выглядит успехом — warning в ответе /sync, счётчик repositories в /health, подсказка на матрице.
- #18 (v1.1): слепая зона (FR-6) и хроники «студенты в тишине» (FR-7) на матрице; auth_failed — баннер «обнови токен»; skipped_rate_limit — «не проверялось». Тестов 120 → 140.
- .gitignore дополнен сайдкарами SQLite (*.db-shm/*.db-wal) — случайно закоммиченные файлы вычищены из истории стека до merge.

## 2026-07-28
- Первая итерация реализована целиком: тикеты #6 (S4 config_manager), #9 (G2 sync_orchestrator), #10 (G3 детект заготовок), #11 (G4 свод-реконсиляция), #12 (D1 матрица), #13 (I2 /health + cron), #14 (D4 карточка студента), #16 (O2 Override UI) — стеком веток на main, каждый с tests-first циклом и ревью APPROVE. Тестов 36 → 120.
- README: раздел «Деплой» (crontab §5.5, чек-лист); «Статус» актуализирован. ARCHITECTURE §3.1: роуты dashboard.py дополнены (/students/{id}, /verdicts/{id}/override-toggle). Старый смоук `/health` заменён suite'ом test_health.py (контракт расширен тикетом #13).

## 2026-07-21
- Документация приведена к реальности main после аудита дрейфа занятия 10: README.md перегенерирован из кода, INDEX.md / plans/plan.md / memory/MEMORY.md синхронизированы (было: «кода нет», «PRD v2.2» — стало: 7 закрытых тикетов в main, PRD v2.4).
- MEMORY.md анонимизирован: фамилии студентов заменены кодами С-01…С-11 (маппинг — во внутреннем контуре курса, не в этом репозитории).
- CLAUDE.md: добавлен раздел «Стандарт документации» (4 правила); чек-лист агента-ревьюера дополнен пунктом 6 — проверка стандарта документации по диффу (задание З3 занятия 10).

## До 2026-07-16
- История ранних изменений — в сообщениях коммитов и `memory/MEMORY.md` (разделы «Сделано …»).
