# Ревью — пятая итерация (доводки по фокус-группам): C5, D23, D24, D36–D39 + дизайн-проход D40 (ветка C4-iter4)

**Дата:** 2026-07-31 · **Ревьюер:** агент-ревьюер (чистый контекст) · **Проверяемое:** диапазон `7ef8da6..00867ee` (итерация 5: пары tests-first → реализация для D23/D24/D36/D37/D38+D39, конфиг-коммит C5, дизайн-проход D40, docs-коммиты; worktree `../cd-iter4`). Спека — `plans/доводки-ux-5-2026-07-31.md`. Коммиты доработки по ревью итерации 4 (`b1fe3f3`, `f55d4eb`) и фиксы живой петли (`0cd94f0`, `c998133`) в диапазон попадают, но ревьюются здесь только как контекст.
**Линзы:** соответствие AC спеки (включая негативные), честность TDD (красная фаза, контракт существующих тестов, tests-change), слои routes → services → store, append-only журнал, миграция `e9d23a5c1f04` (снятие/восстановление триггеров И5, fail-safe), NFR-2, XSS в новых шаблонах, синхронность документации.
**Известные решения CEO — не находки:** механизм чистки журнала версионированной миграцией (снятие триггеров И5 санкционировано явно); bump версий рубрик без смены текста (C5); «погашенных: 0» (отмена негативного AC D19); дизайн-токены D40 при нетронутых статусных подложках.

**Вердикт: APPROVE по всем восьми тикетам** (D36 и D39 — с оговорками).
Блокеров нет. Сьюта на HEAD — **411 passed**, `ruff check .` — чисто. Одна серьёзная находка (1 — границы периода работы в шапке-резюме дела считаются и показываются в UTC: тот же класс ошибки, что находка 1 ревью итерации 4, на том же экране) — забрать до защиты 20.08. Остальное — замечания.

| Тикет | Вердикт | Комментарий |
|---|---|---|
| D23 — дубль artifact_def | **APPROVE** | Миграция снимает ровно 2 из 4 триггеров И5, восстанавливает в `finally` (закреплено тестом), fail-safe и идемпотентность покрыты; runtime-guard журнал не трогает. Оговорка — находка 3 (README без слова «бэкап») |
| D24 — «перемещён» + MR-события | **APPROVE** | Схлопывание пары, MR-события с пометкой «дата хостинга», негативные сценарии на месте; находка 7 — зависимость от порядка сканирования |
| D36 — резюме + «погашенных: 0» + /defense | **APPROVE с оговоркой** | Все 3 пункта AC на месте; забрать находку 1 (UTC в границах периода) до защиты; находка 6 — счётчик разрывов шире показанного списка |
| D37 — русские имена ролей | **APPROVE** | ROLE_TITLES/role_title — один факт в labels.py; сырых слагов в шаблонах не осталось; якорь D17 закреплён регресс-тестом |
| C5 — принудительный пересчёт | **APPROVE** | Диф — ровно 9 строк `version:` (+0.1), тексты не тронуты (проверено дифом); golden set 2/2 записан с моделью; старые вердикты в журнале |
| D38 — P1-пакет | **APPROVE** | Все 5 пунктов реализованы и покрыты; находка 4 — тест обрезки по слову фактически не проверяет границу слова |
| D39 — P2-полировка | **APPROVE с оговоркой** | 6 из 7 пунктов сделаны; колонка «Коммит» не русифицирована (находка 2) |
| D40 — дизайн-проход | **APPROVE** | Токены аддитивны, статусные подложки не тронуты (подтверждено дифом CSS), сигнатура «титульный лист» — чистый CSS без разметки данных |

---

## Что проверено и сошлось

- **TDD честный по всем тикетам с кодом.** Красная фаза прогнана в клоне на каждом tests-first коммите: D23 `a2df122` — 5 failed / 0 passed; D24 `2c2512e` — 2 failed / 1 passed; D36 `44a3fda` — 7 failed / 19 passed (19 зелёных — существующие тесты `test_defense_v2`); D37 `a81dce5` — 3 failed / 0 passed; D38/D39 `9b529e7` — 9 failed / 2 passed (зелёные — находки 4, 5). Единственное касание app-кода в tests-first коммите (`a81dce5`, artifact_matrix.py) — перестановка двух import-строк под линтер, поведение не менялось. C5 — конфиг и данные, кода нет: tests-first неприменим, гейт — golden set.
- **Существующие тесты — контракт: соблюдён.** Все правки существующих тестов несут строку `tests-change` с обоснованием и ссылкой на решение: `test_defense_v2` (явный ноль погашенных — решение CEO №3), `test_override_ui`/`test_mr_channel_note`/`test_artifact_row_summary` (формулировки — сами AC D38/D39), `test_deferred_ui` (реплика Jinja-глобалей — инфраструктура). Переписывание красных тестов D23 внутри той же итерации (reconcile → миграция) также помечено tests-change и покрывает те же требования.
- **Миграция `e9d23a5c1f04`** (механизм санкционирован CEO — ревьюется только качество): ревизует актуальную голову `c7d19a4e5b02`; снимает **ровно два** триггера И5 (`trg_verdict_no_update`, `trg_snapshot_no_delete`) — минимум, необходимый для repoint вердиктов и удаления снапшотов дубля; `trg_snapshot_no_update` и `trg_verdict_no_delete` остаются боевыми на всё время миграции. Восстановление — в `finally`, безусловно, SQL восстановленных триггеров семантически идентичен исходным из `d408a1d4f3e7`. Fail-safe: группа без эквивалента по content_hash пропускается целиком (провенанс вердикта не рвётся) — закреплено тестом `test_migration_failsafe_no_equivalent`; восстановление триггеров закреплено assert'ом по `sqlite_master`. Хранимым остаётся определение с самым ранним наблюдением; вердикты перепривязываются на эквивалент того же content_hash (четвёрка Б1 не меняется — проверено тестом). `downgrade` — честный no-op (чистка данных необратима by design, схема не менялась).
- **Append-only вне миграции не нарушен:** runtime-guard `config_dedupe_artifact_defs` удаляет только определения **без наблюдений** (artifact_def — конфиг, И5 его не покрывает; журнал не затронут), дубли со снапшотами — warning с отсылкой к миграции; идемпотентность и «разные паттерны — не дубль» покрыты тестами.
- **Слои чистые:** новый роут `GET /defense` — тонкий (auth-гейт + проекция реестра), вся логика D24/D36 — в `evidence_chain.py` (`_defense_events`, `case_summary`, переехавшие `merged_no_review_count`/`repo_progress` — дубль правила «мимо ревью» из находки 3 ревью итерации 4 устранён), словари D37/D38/D39 — в `labels.py` (один факт в одном месте), обрезка по слову — в `artifact_matrix.py` до шаблона.
- **XSS:** `|safe` в шаблонах отсутствует (grep по всем — ноль), autoescape активен; в новых поверхностях (`defense_index.html`, легенды, title-подсказки колонок, `from_path → file_path` у «перемещён», имя репо в заголовке модалки) данные рендерятся через `{{ }}`; подсказки колонок — статические строки словаря ROLE_HINTS.
- **NFR-2:** пайплайн обхода в этой итерации не менялся (sync_orchestrator не тронут); негативный AC C5 «обход без ключа — пары честно „проверяется“» обеспечен прежним механизмом (вердикта на новую четвёрку нет → state pending), закреплён существующими тестами и README.
- **Негативные сценарии спеки покрыты** (исключения — находки ниже): дублей нет — no-op/идемпотентность; разные паттерны — не дубль; «исчез» без «появился» — остаётся «исчез» (`test_real_disappear_kept`); MR без даты хостинга — не в ленте (`test_mr_events_in_chronology`); `/defense` при пустом реестре — «реестр пуст» + 303 без сессии; нейтральность списка на проекторе — assert'ы на отсутствие «⚠», «X/M», «мимо ревью».
- **D36 нейтральный вход:** `GET /defense` отдаёт только короткое имя репо + ссылку на дело, ссылка из артефактной матрицы есть (тест); teacher-only сохранён (BR-4).
- **C5:** диф `config.yaml` — только 9 строк `version` (+0.1 у всех 9 рубрик рёбер), тексты не менялись; golden-set.md — прогон 2026-07-31 №2 по GS-1/GS-2 с вердиктами и `llm_model: deepseek-v4-flash`; R-3 честно помечена как известная нестабильная пара.
- **Документация в тех же MR:** CHANGELOG — записи по D23, D24/D36/D37, C5, D38/D39/D40; `plans/plan.md` §7 — итерация 5 закрыта; README §Статус переписан на актуальное состояние (заменено, не дополнено); спека итерации закоммичена (`e3adafe`). Исключение — находка 3.

## Находки

| № | Severity | Где | Что |
|---|----------|-----|-----|
| 1 | **серьёзное** | `evidence_chain.py:356-373` (`case_summary`), `defense.html:9` | **Границы периода работы в шапке-резюме — в UTC.** `work_from`/`work_to` собираются из наивных UTC-значений (`row.updated_at`, `snap.source_commit_date or snap.observed_at`) и рендерятся `strftime("%d.%m")` **без `to_display`** — при том, что вся остальная страница после доработки по ревью итерации 4 показывает местное время. Коммит/MR поздним вечером по местному времени (UTC+4: после 20:00) уедет на предыдущие сутки — «работа 19.07 — 30.07» может показать 29.07. Это тот же класс ошибки, что находка 1 ревью итерации 4, на том же экране, чья работа — доказывать даты комиссии («независимые даты против „я сдавал раньше“»). Тест `test_case_summary_values:117-118` фиксирует сырые UTC-значения, то есть закрепляет ошибку контрактом. Чинить дёшево: `to_display()` вокруг элементов `dates` (или на выходе), поправить два assert'а с пометкой tests-change. |
| 2 | замечание | `defense.html:93,123`, `student_card.html:90` | **AC D39 «колонка „Коммит“» не выполнен:** заголовки таблиц хронологии и полного лога остались `Commit` (английский) — единственный пункт спеки итерации без реализации и без теста. Русификация статусов MR (открыт/влит/закрыт) при этом сделана и покрыта. Три строки правки + тест. |
| 3 | замечание | `README.md` | **AC D23 «бэкап перед deploy обязателен (README)» не выполнен:** в README нет ни слова «бэкап»/«резервная копия» (grep — пусто). С переходом чистки на миграцию требование стало только важнее: `alembic upgrade head` на боевой БД снимает append-only триггеры и удаляет снапшоты дублей — без бэкапа откатиться не с чего (downgrade — no-op by design). Добавить строку в §Деплой. |
| 4 | замечание | `tests/test_polish_v5.py:141-150` | **Тест обрезки по слову не проверяет обрезку по слову.** Он зелёный уже на tests-first коммите (прогнано: старое поведение «резать на 80-м символе посреди слова» его проходит): assert «не посреди слова» сверяет суффиксы `"требовани"`/`"каждо"`, которых в посеянной строке `LONG_ENTITY` нет вовсе (обрезок реально кончается на «до ф…»). AC D38 «усечение по границе слова» фактически без регресс-страховки. Чинить: сверять точный ожидаемый обрезок (`"…модерации до…"`-вариант) или `chip[:-1].split()[-1]` на полное слово из исходной строки. |
| 5 | замечание (процесс) | коммиты `9b529e7`, `2c2512e` | Красная фаза неоднородна, пометка «(красные)» неточна: `test_defense_equal_shas_collapsed` зелёный до реализации (литерал `@sha → @sha` в старой разметке не встречался — между хешами стояли имена файлов; guard-ценность для новой разметки есть), `test_real_disappear_kept` — тривиально-зелёный негативный guard. Повтор находки 7 ревью итерации 4: отмечать в сообщении tests-first коммита, какие тесты рождаются зелёными как guard. |
| 6 | замечание | `evidence_chain.py:365-369`, `defense.html:9,18` | Счётчик резюме «разрывов: N» считает непогашенные разрывы **любой** уверенности, а секция ниже показывает только high: при medium/low-разрыве комиссия увидит «разрывов: 2», под этим — один разрыв и «Уверенных разрывов для показа нет». Посев теста содержит только high — расхождение не закреплено ни в одну сторону. Либо считать в резюме только high (симметрия с секцией), либо подписать «из них уверенных: K». |
| 7 | замечание | `evidence_chain.py:288-303` | Схлопывание «перемещён» зависит от порядка переходов внутри обхода: «появился» ищет «исчез» только среди **уже пройденных** событий. Если в одном обходе снапшот нового пути наблюдён раньше снапшота старого (порядок сканирования artifact_def — например, перенос из `DISCOVERY.md` обратно в `product/interviews/`), пара не схлопнется и флаппинг вернётся. Негативного теста на обратный порядок нет. Чинить — вторым проходом или поиском пары в обе стороны в пределах sync_run. |

**Мелочи (без номера):** комментарии `# v1.2 (решение CEO 2026-07-30 №2)` в config.yaml остались у рубрик с `version: "1.3"` — устаревшая пометка рядом с фактом; формулировка резюме «MR: K, мимо ревью: J · работа X — Y» против спековой «MR: K, из них мимо ревью: J · работа с X по Y» — смысл сохранён, косметика; локальный `import repo_short_name` внутри `defense_index` при уже существующем Jinja-глобале; негативный AC D36 «нет MR и нет снапшотов — резюме с прочерками» не покрыт тестом (код не падает — период просто скрывается, что мягче спеки); tests-change коммита `e02d82f` не упоминает правку собственного `test_polish_v5` (тест этой же итерации — допустимо, но для симметрии стоило).

## Что заслуживает действия

1. **До защиты 20.08:** находка 1 — `to_display` для границ периода + tests-change двух assert'ов. Единственная находка, бьющая по смыслу тикета (доказательные даты перед комиссией).
2. Находки 2–4 — дешёвые (строки), естественно забрать одним коммитом-хвостом; 6 — решить формулу счётчика до первой защиты с medium-разрывом; 5, 7 — при следующем касании файлов.

## Прогоны (клон в scratchpad, рабочее дерево не тронуто; venv `../course-dashboard/.venv`)

| Точка | Команда | Результат |
|---|---|---|
| HEAD `00867ee` | `pytest -q` | **411 passed** (2:40) |
| HEAD `00867ee` | `ruff check .` | чисто |
| `a2df122` (D23 tests first) | `pytest tests/test_config_dedupe.py -q` | красный: 5 failed / 0 passed |
| `2c2512e` (D24 tests first) | `pytest tests/test_defense_chronology_v5.py -q` | красный: 2 failed / 1 passed (см. находку 5) |
| `44a3fda` (D36 tests first) | `pytest tests/test_defense_case_v5.py tests/test_defense_v2.py -q` | красный: 7 failed / 19 passed (зелёные — существующие тесты defense_v2) |
| `a81dce5` (D37 tests first) | `pytest tests/test_role_titles_v5.py -q` | красный: 3 failed / 0 passed |
| `9b529e7` (D38/D39 tests first) | `pytest tests/test_polish_v5.py -q` | красный: 9 failed / 2 passed (см. находки 4, 5) |
| миграция | тесты `test_migration_*` (upgrade `c7d19a4e5b02` → head на посеянной БД) | дедуп + repoint + триггеры восстановлены; fail-safe без эквивалента — группа не тронута |
