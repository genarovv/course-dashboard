# Ревью — тикет FR-11-пилот (#45): рубрика качества README, скрипт вне продукта (ветка fr11-pilot)

**Дата:** 2026-07-31 · **Ревьюер:** агент-ревьюер (чистый контекст) · **Проверяемое:** коммиты с префиксом «FR-11-пилот» на ветке `fr11-pilot`: `f8e8621` tests first → `d0391c7` реализация → утверждение рубрики (правка `evals/fr11_pilot.py` внутри merge-коммита `ee9e0db`, см. находку 7); файлы `evals/fr11_pilot.py`, `tests/test_fr11_pilot.py`, `.gitignore`, `CHANGELOG.md`, `tests/MAP.md`. Остальные коммиты ветки (D15–D18) — чужая итерация, не ревьюились.
**Контекст:** решения CEO 2026-07-09/28 — FR-11 в продукт v1 не строим; пилот = скрипт по образцу мини-эвала (C3 #34), материал занятия 15 + данные точности для решения о FR-11 в v2. Рубрика КАЧЕСТВА README v1.0 (5 критериев) утверждена CEO 2026-07-31. Боевой прогон состоялся: 1 «годно», 7 «с оговорками», 1 «README не найден».
**Линзы:** изоляция от продукта, секреты, строгость валидатора, механика вердикта, приватность меток, фиксация модели/версии рубрики, устойчивость прогона, gitignore, честность tests first, качество тестов.

**Вердикт: APPROVE с оговорками.**
Блокеров нет: скрипт изолирован от продукта, секрет не течёт, отчёт с цитатами студентов надёжно вне git, метки анонимны, модель и версия рубрики зафиксированы, tests first честный, прогоны зелёные. Оговорки — три находки (1–3), искажающие именно то, ради чего пилот существует — замер точности; забрать их до следующего прогона / занятия 15. Merge-состояние ветки они не блокируют.

---

## Что проверено и сошлось

- **Скрипт вне продукта.** `d0391c7` не трогает `app/`; `git grep fr11 -- app/` на `ee9e0db` пуст — приложение скрипт не импортирует. Зависимость односторонняя `evals/` → `app/` (переиспользует `GitClient`, `store`, `settings`) — тот же допустимый паттерн, что у мини-эвала.
- **Секрет не в логах и не в отчёте.** `deepseek_api_key` уходит только в заголовок `Authorization` (`fr11_pilot.py:195`); в `print` попадают метка репо и `exc` — у `httpx.HTTPStatusError` в тексте URL и статус, заголовков нет; сообщения `GitClientError` (`git_client.py:224-236`) токенов не содержат. `SystemExit` при отсутствии ключа — без значения ключа. В отчёте секрета нет (проверен реальный отчёт).
- **Строгий JSON-валидатор** (`fr11_pilot.py:62-84`): fence-блок извлекается, не-dict/мусор → `None`, `verdict` строго из домена `("годно", "с оговорками", "негодно")`, `criteria` — только список, каждый элемент — dict с непустым `key` и строго булевым `met` (`isinstance(item["met"], bool)`; строка «да» отклоняется — тест есть). Строгость по количеству критериев — см. находку 4.
- **Метки репо — без ФИО.** `repo.id` — uuid4 (`app/models/repository.py:14`), метка `репо-NN (uuid[:8])` (`fr11_pilot.py:159`) анонимна; `repo_url` в отчёт не попадает. Проверено по реальному отчёту `evals/отчёт-fr11-пилот-2026-07-31.md`: только `репо-01 (81d04cf4)` и т.п., ни фамилий, ни ников, ни URL. URL с ником студента может мелькнуть в консоли при `GitClientError` — консоль эфемерна, в артефакты не пишется; допустимо.
- **Модель и версия рубрики зафиксированы.** `llm_model` в отчёте — та же `settings.deepseek_model`, что ушла в запрос (конструктивно не разойдутся, канон Б1); `RUBRIC_VERSION = "1.0"` в шапке отчёта; тест `test_report_fixes_model_and_rubric_version` это закрепляет. Детерминизм: `temperature: 0.0`, `response_format: json_object`.
- **«README не найден» и недоступный репозиторий не роняют прогон:** снапшот отсутствует или без `content_hash` → строка «README не найден» и `continue` (`fr11_pilot.py:172-175`); `GitClientError` ловится по-репозиторному (`176-185`); `httpx.HTTPError` LLM — тоже (`200-203`); `git.aclose()` в `finally`. Про качество этих веток — находки 2, 3, 5.
- **Отчёт в .gitignore.** Паттерн `evals/отчёт-fr11-пилот-*.md` (`.gitignore:23`) совпадает с генерируемым именем; `git check-ignore` в клоне подтверждает срабатывание на кириллическом имени; `git ls-tree` по всем коммитам — отчёт нигде не закоммичен; в `git status` рабочего дерева реальный отчёт не всплывает.
- **Tests first честный.** `f8e8621` — только `tests/test_fr11_pilot.py`; в его дереве `evals/fr11_pilot.py` отсутствует (`git ls-tree` — только golden-set.md и mini_eval.py). Прогон в клоне на `f8e8621`: `ModuleNotFoundError: No module named 'evals.fr11_pilot'` — красная фаза реальна, ровно как заявлено в сообщении коммита («красные: модуля нет»).
- **Прогоны в клоне (рабочее дерево не тронуто):** на `d0391c7` и на HEAD ветки `pytest tests/test_fr11_pilot.py -q` — **8 passed**; `ruff check evals/fr11_pilot.py tests/test_fr11_pilot.py` — чисто. Дельта тестов +8 (368 → 376 по `--collect-only`) сходится с восемью новыми тестами; про абсолютные числа в CHANGELOG — находка 8.
- **Боевой прогон сверен с кодом.** Профиль шаблонных README ✅❌✅❌✅ (репо-05…09, 3 met) → «с оговорками» — по правилу вердикта корректно; репо-04 5/5 → «годно», репо-01/02 4/5 → «с оговорками». В этом прогоне модель правило соблюла (проверено по каждой строке отчёта), но код этого не гарантирует — находка 1.
- **Доки в том же MR:** CHANGELOG-запись и строка в `tests/MAP.md` — в `d0391c7`; утверждение рубрики отражено в самом файле (комментарий и шапка отчёта: «утверждена решением CEO 2026-07-31»). Про README и трассируемость утверждения — находки 6, 7.

---

## Находки

| № | Severity | Где | Что |
|---|----------|-----|-----|
| 1 | **важно** | `fr11_pilot.py:55` (промпт), `62-84` (валидатор), `211-215` | **Правило вердикта — механическое, но живёт только в промпте.** «Все 5 met — годно; 3–4 — с оговорками; 0–2 — негодно» — чистая арифметика по `met`, однако вердикт берётся из ответа модели как есть: модель может вернуть «годно» при 2 met, и валидатор это пропустит (он проверяет только домен слова). Для пилота, чей продукт — *данные точности*, это загрязнение замера: расхождение вердикта с собственными критериями модели запишется как «мнение модели» и уедет в ручную сверку преподавателя неотличимым от честной оценки. В боевом прогоне модель правило соблюла (сверено по всем 9 строкам), но это удача temperature=0, а не гарантия. Чинить дёшево: считать вердикт в коде из `criteria` (правило и так механическое), а вердикт модели либо игнорировать, либо сверять и помечать расхождение в отчёте отдельной колонкой — само по себе полезный сигнал качества модели для занятия 15. |
| 2 | важно | `fr11_pilot.py:205-208` | **Ответ модели не распарсился → вердикт «негодно»** — инфраструктурный сбой записывается как оценка качества README студента. Проект уже решал этот вопрос канонически: §5.2 / мини-эвал → `deferred(parse_error)` c одним ретраем; здесь ни ретрая, ни отдельного состояния. В таблице такая строка показала бы «негодно» с прочерками «?» — преподаватель при сверке рискует засчитать это как оценку. В боевом прогоне кейс не встретился, но для повторных прогонов (v2, другие модели) чинить: отдельное состояние «не оценено (ответ не распарсился)» + опциональный 1 ретрай по образцу мини-эвала. |
| 3 | среднее | `fr11_pilot.py:200-203` + `render_report:103-104, 112-114` | **«LLM недоступна» в отчёте маскируется под «README не найден».** Ветка httpx-ошибки кладёт `verdict=None, notes="LLM недоступна"`, но рендер для всех `verdict is None` пишет в таблицу «README не найден», а детальная секция такие строки пропускает — пометка «LLM недоступна» существует только в консоли и в отчёт не попадает вовсе. Два принципиально разных факта («у студента нет README» vs «мы не смогли спросить модель») неразличимы в единственном артефакте пилота. Чинить: третий вариант строки таблицы по `notes` (одна ветка в `render_report`). |
| 4 | мелочь | `fr11_pilot.py:76-84` vs промпт `:50` | Промпт требует «ровно 5 объектов» с ключами из домена, валидатор принимает список любой длины (включая пустой — цикл просто не выполнится) и любые непустые `key` без проверки домена и дубликатов. `render_report` не падает (недостающие ключи → «?»), но правило вердикта осмысленно только при 5 критериях. Тестовая фикстура `VALID` с двумя критериями эту мягкость закрепляет как норму. Если делать находку 1 (счёт по `met` в коде) — проверка «ровно 5, ключи из домена, без дублей» становится обязательной частью того же фикса. |
| 5 | мелочь | `fr11_pilot.py:199` | `response.json()["choices"][0]["message"]["content"]` — `JSONDecodeError`/`KeyError`/`IndexError` от кривого 200-ответа не перехватываются (`except` ловит только `httpx.HTTPError`) → уронит весь прогон, потеряв уже собранные строки. Тот же класс, что находка 2 ревью C3 (там починили). Одна строка: расширить except или обернуть извлечение. |
| 6 | мелочь | `fr11_pilot.py:149`; `README.md:64` | Два в одном: (а) `select(Repository)` без фильтра `archived_at IS NULL` — боевой конвейер (FR-8, `store.py:284`) обходит только активные репозитории, пилот оценит и архивные; сейчас архивных нет, но семантика «последние боевые снапшоты» дрейфует. (б) `README.md:64` описывает `evals/` как «Golden-set + мини-эвал» — появился второй скрипт, строка не обновлена в том же MR (стандарт документации п.1; та же находка 7 была в ревью C3). |
| 7 | мелочь (процесс) | merge `ee9e0db` | Утверждение рубрики CEO (правка комментария и шапки отчёта «на утверждении» → «утверждена 2026-07-31») зафиксировано не отдельным коммитом «FR-11-пилот: …», а внутри merge-коммита чужой итерации D15-p1. Содержательно правка верна (сверено: только 2 строки в `evals/fr11_pilot.py`), но `git log --grep="FR-11-пилот"` её не находит — трассируемость решения CEO по прецеденту #42 хуже, чем могла быть. На будущее: решения CEO — отдельным коммитом со своим префиксом. |
| 8 | мелочь | `CHANGELOG.md:6` | «Тестов 277 → 285» не воспроизводится: `--collect-only` даёт 368 на точке ветвления (`47432f7`) и 376 после `d0391c7`. Дельта +8 верна, абсолютные числа взяты, судя по всему, от устаревшей базы (до вливания D8/D9-D14). Ревью C3 задало конвенцию сверять эти числа — здесь она нарушена. |

## Что заслуживает действия

1. **До следующего прогона / занятия 15:** находки 1–3 — все локальны в `evals/fr11_pilot.py` (+ несколько тестов), один небольшой коммит. Именно они определяют, можно ли доверять «данным точности», ради которых пилот затевался.
2. Находки 4–5 естественно забрать тем же коммитом; 6–8 — хвостом или при следующем касании файлов.

## Прогоны (клон, рабочее дерево не тронуто)

| Точка | Команда | Результат |
|---|---|---|
| `f8e8621` (tests first) | `pytest tests/test_fr11_pilot.py -q` | красный: `ModuleNotFoundError` (модуля нет — как заявлено) |
| `d0391c7` (реализация) | `pytest tests/test_fr11_pilot.py -q` | **8 passed** |
| `d0391c7` | `ruff check evals/fr11_pilot.py tests/test_fr11_pilot.py` | чисто |
| HEAD `fr11-pilot` (`ee9e0db`) | `pytest tests/test_fr11_pilot.py -q` + ruff | **8 passed**, чисто |
| `.gitignore` | `git check-ignore evals/отчёт-fr11-пилот-2026-07-31.md` | срабатывает (`.gitignore:23`), кириллица не мешает |
