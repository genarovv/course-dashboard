# Ревью — четвёртая итерация (UX-доводки): C4 #64, D19 #65, D20 #66, D21 #67, D22 #68 (ветка C4-iter4)

**Дата:** 2026-07-31 · **Ревьюер:** агент-ревьюер (чистый контекст) · **Проверяемое:** диапазон `c13695a..7ef8da6` (13 коммитов: 5 пар tests-first → реализация + 3 общих; worktree `../cd-iter4`). Спека — `plans/доводки-ux-2026-07-31.md`.
**Линзы:** соответствие AC спеки (включая негативные сценарии), честность TDD (красная фаза, контракт существующих тестов, tests-change), слои routes → services → store, append-only журнал, NFR-2, XSS в шаблонах, миграция `c7d19a4e5b02` (upgrade + downgrade), синхронность документации.

**Вердикт: APPROVE по всем пяти тикетам.**
Блокеров нет. Сьюта на HEAD — **379 passed**, `ruff check .` — чисто. Одна серьёзная находка (1 — часовые пояса в хронологии защиты D19: экран, чья работа — доказывать даты, показывает UTC вперемешку с локальными вехами) — забрать до защиты 20.08. Остальное — замечания.

| Тикет | Вердикт | Комментарий |
|---|---|---|
| C4 #64 — русский выход ядра | **APPROVE** | Инструкция в промпте, контрактный тест, валидатор не тронут, гейт golden set 2/2 зафиксирован с датой и моделью |
| D19 #65 — дело защиты v2 | **APPROVE с оговоркой** | Все 5 пунктов AC на месте; забрать находку 1 (UTC в хронологии) до защиты 20.08 |
| D20 #66 — сводная колонка + сортировки | **APPROVE** | Контракт D15 перенесён с пометкой tests-change по спеке; находки 3, 5 — замечания |
| D21 #67 — честная ячейка + курирование | **APPROVE** | «сдано, но разрыв» только для found (спека адресует только «есть»), чип ~80 симв + line-clamp, кнопка с «отметка обратима» |
| D22 #68 — полировка | **APPROVE** | Все 7 пунктов AC покрыты тестами; находки 2, 8, 9 — замечания |

---

## Что проверено и сошлось

- **TDD честный по всем пяти тикетам.** Каждый tests-first коммит трогает только `tests/` (проверено `git show --stat`); реализация на этих коммитах отсутствует статически (grep по пяти ключевым маркерам — нули) и динамически: прогон в клоне на каждом tests-first коммите красный — C4 `714d7d6`: 1 failed / 15 passed; D19 `216bbc9`: 15 failed / 2 passed; D20 `49a3f14`: 8 failed / 4 passed; D21 `d19fbc1`: 4 failed / 2 passed; D22 `9d24767`: 8 failed / 5 passed (зелёные — существующие тесты тех же файлов и тривиально-негативные, см. находку 7).
- **Существующие тесты не ослаблены.** Изменены ровно два: `test_artifact_row_breaks.py` (1 assert: правая колонка → сводка строки) и `test_artifact_legend.py` (формулировки легенды). Оба — со строкой `tests-change` в сообщении коммита и docstring с обоснованием; оба изменения требования явно санкционированы спекой (D20: «переносятся на новую колонку с пометкой tests-change»; D22: формулировки легенды — это и есть AC). Контракт D18 (`test_defense_mode.py` — нет кнопок FR-10, нет данных группы) не тронут; `override-toggle` в защите отсутствует — закреплено тестом `test_muted_breaks_section`.
- **Миграция `c7d19a4e5b02`** ревизует актуальную голову `b3f1a92c7d10`, единственная голова цепочки; nullable-колонка через `batch_alter_table` (SQLite). Прогнал upgrade → head и downgrade −1 на чистой БД: колонка появляется и корректно удаляется.
- **Append-only журнал не нарушен:** изменения только добавляют (новая nullable-колонка, новые снапшоты в обходе, чтение override.created_at); ни одного update/delete по снапшотам и вердиктам; C4 старые англоязычные вердикты в БД не трогает — как в спеке.
- **NFR-2:** сбой даты коммита → NULL, обход завершается (`test_sync_date_failure_gives_null_not_crash`, статус completed); `_head_commit_date` (sync_orchestrator.py:154-167) ловит `GitClientError`, `_parse_host_datetime` — `ValueError`. Краевой случай кривого JSON — находка 4.
- **Слои чистые:** роут `/artifacts` лишь расширил допустимые значения sort (dashboard.py:180); вся логика сводки/сортировок — в `artifact_matrix.py`, хронологии — в `evidence_chain.py`, сбора даты — в `sync_orchestrator.py`; запросы — через `store.*`. Шаблонные глобалы (`fmt_dt`, `repo_short_name`) подключены в одном месте (routes/__init__.py).
- **XSS:** Jinja autoescape активен (стандартные `Jinja2Templates`); LLM-текст (`notes`, `entity`, `quote`, `why`) и URL рендерятся через `{{ }}` без `|safe`; усечение сущности (artifact_matrix.py:108-109) происходит до шаблона и не размечает строку как safe; inline-JS кнопки «обновить сейчас» — статический, без подстановки данных.
- **Негативные сценарии спеки покрыты** (кроме двух — находки 5, 6): API не отдал дату → None (`test_head_commit_date_missing_in_answer_is_none`); репо без MR / без погашенных → секций нет; пустая строка первой при sort=lag; одна пустая MR-колонка не схлопывается; занятая MR-колонка живёт отдельно; смена контента без смены статуса — не переход; первый not_found перехода не рождает; короткая сущность не усекается; «частично» не переписано на «сдано, но разрыв» (спека адресует только «есть»).
- **POST /sync с кнопки** защищён как раньше: сессия преподавателя или X-Sync-Token (admin.py:49-61, BR-4); кнопка шлёт fetch только после `confirm(...)` — закреплено тестом.
- **Документация в тех же MR:** CHANGELOG — 5 записей (по одной на тикет); `data-model.md` §1.9 — строка `source_commit_date` с семантикой NULL; `ARCHITECTURE.md` §5.1 — шаг `get_head_commit_date` в обходе; `evals/golden-set.md` — прогон 2026-07-31 по GS-1/GS-2 с вердиктами, счётчиками и `llm_model: deepseek-v4-flash`. PRD не тронут — по спеке (детализация FR-5/FR-13, не новые требования).
- **Известные решения** (в находки не выношу, зафиксированы в коде/спеке): второй запрос того же эндпоинта в `get_head_commit_date` — осознанная цена неизменности контракта `get_head_sha`; getattr-фолбэк в `_head_commit_date` для старых фейков; язык выхода LLM не валидируется схемой — инструкция промпта, деградация приемлема.

## Находки

| № | Severity | Где | Что |
|---|----------|-----|-----|
| 1 | **серьёзное** | `evidence_chain.py:200, 224, 270`; `defense.html` | **Хронология защиты показывает UTC и мешает его с локальными датами.** Переходы берут `source_commit_date or observed_at` (наивный UTC из БД) и рендерятся `fmt_dt(ev.when)` без `to_display`; вехи занятий — `lesson.date` в полночь (локальная дата курса); `opened_at` на той же странице — уже через `to_display`. Конвенция #32 («в БД наивный UTC, показываем местное время» — так делает as_of в `matrix_builder.py:165-168`) нарушена ровно на экране, чья задача — доказывать даты: коммит в 01:30 местного (UTC+4) покажется предыдущим днём 21:30, и «тесты появились через 4 дня после занятия 11» может съехать на день. Полный лог и карточка студента (`fmt_dt(entry.observed_at)`) — та же история. Чинить дёшево: прогонять `when`/`observed_at` через `to_display` перед `fmt_dt` (вехи занятий уже локальные — их не трогать). |
| 2 | замечание | `matrix_builder.py:188-199` | Подпись схлопнутой колонки `занятия {min}–{max}` врёт при несмежных пустых MR-занятиях: 11 и 13 пустые, 12 с файлом → заголовок «занятия 11–13: сдача через MR», а колонка 12 живёт отдельно. Сценарий реален (студенты сдают файлы в середину MR-блока). Теста на несмежность нет; чинить — схлопывать только смежные интервалы или перечислять номера. |
| 3 | замечание | `artifact_matrix.py:249-253` vs `matrix_builder.py:160` | Правило «мимо ревью» (state == merged и не reviewer_approved, порядок CEO 2026-07-29) теперь живёт в двух местах дословным дублем. «Один факт в одном месте»: вынести предикат в общий хелпер (store или labels), иначе следующая правка порядка разъедет матрицы. |
| 4 | замечание | `git_client.py:129` | `(data.get("commit") or {}).get("committer", {}).get("date")` — при `"committer": null` в JSON получится `None.get("date")` → `AttributeError`, который не `GitClientError`: `_head_commit_date` его не поймает, и упадёт весь `run_sync` — вопреки негативному AC «API не отдал дату — NULL, не падение». Практически маловероятно (git-уровневый committer почти всегда есть), но защита — одна строка (`or {}` после второго `.get`). |
| 5 | замечание | `test_artifact_row_summary.py:190-203` | `test_sort_lag_stable_for_equal_ratio` не проверяет заявленное: в посеве доли 1.0 / 0.5 / 0.0 — равных X/M нет, assert вырождается в «список отсортирован». Негативный AC спеки «обе сортировки стабильны к порядку реестра» для sort=lag фактически не покрыт (для sort=breaks покрыт старым тестом D15). Добавить в посев два репо с равной долей и проверить порядок реестра. |
| 6 | замечание | `tests/test_defense_v2.py` | Негативный сценарий спеки «все снапшоты одного коммита — хронология честно показывает один хеш» без выделенного теста. Поведение следует из дизайна (sha берётся из снапшота как есть), но регресс-страховки нет. |
| 7 | замечание (процесс) | коммит `216bbc9` | Красная фаза D19 неоднородна: 2 из 17 тестов (`test_no_muted_breaks_no_section`, `test_no_mrs_no_section`) зелёные уже на tests-first коммите — негативные «секции нет» тривиально проходят до реализации. Сообщение «(красные)» чуть неточно; сами тесты осмысленны после реализации (упадут, если секции начнут рисоваться без данных). На будущее: отмечать в сообщении, какие тесты рождаются зелёными как guard. |
| 8 | замечание | `student_card.html:73` | Дата MR в карточке студента — `mr.updated_at[:10]` → «2026-07-20»: микросекунд нет (букве AC D22 соответствует), но формат ГГГГ-ММ-ДД выбивается из введённого той же итерацией единого ДД.ММ ЧЧ:ММ (`fmt_dt`). Одно касание: `fmt_dt(mr.updated_at)`. |
| 9 | замечание | `artifact_matrix.html`, `matrix.html` (кнопка sync-now) | `fetch('/sync', …).then(reload)` без обработки отказа: при сетевой ошибке promise отклонён — кнопка навсегда «обход идёт…» без перезагрузки; при 401 (протухшая сессия) reload честно уведёт на логин. Добавить `.catch`/`finally` с возвратом кнопки в исходное состояние. |

## Что заслуживает действия

1. **До защиты 20.08:** находка 1 — точечная правка `evidence_chain.py` + `student_card.html` (прогнать отображаемые времена через `to_display`), плюс тест на смещение зоны. Это единственная находка, бьющая по смыслу тикета D19 «хронология доказательная».
2. Находки 3–5, 8, 9 — дешёвые, естественно забрать одним коммитом-хвостом итерации; 2 и 6 — при следующем касании соответствующих файлов.

## Прогоны (клон в scratchpad, рабочее дерево не тронуто; venv `../course-dashboard/.venv`)

| Точка | Команда | Результат |
|---|---|---|
| HEAD `7ef8da6` | `pytest -q` | **379 passed** (2:29) |
| HEAD `7ef8da6` | `ruff check .` | чисто |
| `714d7d6` (C4 tests first) | `pytest tests/test_llm_client.py -q` | красный: 1 failed / 15 passed |
| `216bbc9` (D19 tests first) | `pytest tests/test_defense_v2.py -q` | красный: 15 failed / 2 passed (см. находку 7) |
| `49a3f14` (D20 tests first) | `pytest tests/test_artifact_row_summary.py tests/test_artifact_row_breaks.py -q` | красный: 8 failed / 4 passed |
| `d19fbc1` (D21 tests first) | `pytest tests/test_cell_honest_curation.py -q` | красный: 4 failed / 2 passed |
| `9d24767` (D22 tests first) | `pytest tests/test_polish_d22.py tests/test_artifact_legend.py -q` | красный: 8 failed / 5 passed |
| миграция | upgrade → head, downgrade −1 (чистая SQLite) | колонка `source_commit_date` появляется и удаляется корректно |
