# Аудит merge «D7-artifact-matrix» (независимое ревью пост-фактум)

**Дата:** 2026-07-30
**Объект:** merge-коммит `4a83552` в main — матрица «репозиторий × артефакт» по макету CEO (GET /artifacts, цветокодированные ячейки, модалка деталей HTMX, стили app.css). Ветка: `7cb3680` (tests first) → `1279ab7` (реализация) → `497e03d` (MEMORY).
**Ревьюер:** независимая сессия аудита (не автор кода). Рабочее дерево не менялось; прогоны — в клоне main в scratchpad.

## Вердикт: ЗАМЕЧАНИЯ

Серьёзных проблем нет. Работа качественная: TDD подтверждён историей коммитов, слои и контракт store соблюдены, BR-4 закрыт, старая матрица FR-4 не сломана, XSS-экранирование на месте. Два замечания по документации/трассируемости, остальное — необязательные рекомендации.

## Находки

1. **[Medium] ARCHITECTURE.md не обновлён тем же MR — и до сих пор.**
   `ARCHITECTURE.md` §3.1 поимённо перечисляет все файлы `services/` (строки ~148–156) и роуты `dashboard.py` (строка ~164); §5.3 описывает карту роутов. D7 добавил `app/services/artifact_matrix.py` и два роута (`GET /artifacts`, `GET /artifacts/{repository_id}/{role}`) — в ARCHITECTURE.md ни одного упоминания `artifact_matrix` нет даже на текущем HEAD main (`grep -c artifact_matrix ARCHITECTURE.md` → 0). Обоснования «докам обновление не требуется» тоже нет. Нарушено: CLAUDE.md «Стандарт документации» п.1 («Код и документация — один MR… ARCHITECTURE.md обновляются в том же MR») и п.4. README.md, CHANGELOG.md, tests/MAP.md при этом обновлены корректно.
   Файлы: `ARCHITECTURE.md`, `app/services/artifact_matrix.py`, `app/routes/dashboard.py`.

2. **[Low] Тикета на D7 нет ни в plans/plan.md, ни в GitHub Issues.**
   Чеклист ревьюера п.1 требует acceptance criteria из `plans/plan.md` — D7 там отсутствует (grep пуст). AC зафиксированы только в докстринге `tests/test_artifact_matrix.py`. Ветка и коммиты ID `D7` несут (правило именования соблюдено), а сам хвост честно записан в `memory/MEMORY.md` («тикет в GitHub Issues на D7 не создан — завести и сослаться»). Замечание: хвост известен, но тикет так и не заведён — трассируемость к трекеру пока держится на памяти сессии.

3. **[Info, известный хвост — подтверждён задокументированным] HTMX через CDN.**
   `app/templates/base.html` подключает `unpkg.com/htmx.org@1.9.12` с комментарием «CDN — кандидат на vendoring в static/js»; хвост продублирован в CHANGELOG и MEMORY (vendoring руками, сеть из сессии была заблокирована). Не новая находка. Практическое следствие: без интернета на занятии модалка деталей не откроется — vendoring стоит сделать до ближайшего офлайн-демо.

4. **[Nit, необязательное] Клавиатура не открывает модалку.**
   Ячейка `<td … role="button" tabindex="0" hx-get=…>` (`artifact_matrix.html`) — HTMX по умолчанию вешает на td только click; Enter/Space с клавиатуры ничего не делают, хотя `role="button"` это обещает. Косметика доступности для v1 teacher-only.

5. **[Nit, необязательное] Дублирование best-wins логики.**
   `artifact_matrix._best_snapshot` почти повторяет `evidence_chain._latest_snapshot_for_role` (ранг статуса → свежесть); единственное отличие (матрица не фильтрует по `content_hash` — нужен честный «нет») задокументировано в докстринге. При следующем заходе в этот код стоит свести к одной функции с флагом.

## Что проверено и чисто

- **Tests first (стандарт тестирования п.2):** коммит `7cb3680` содержит только `tests/test_artifact_matrix.py` (+273 строки); прогон на этом коммите в клоне падает на сборке (модуля `artifact_matrix` ещё нет) — красная фаза реальна. Реализация `1279ab7` правит тестовый файл на 8 строк с явным объяснением в сообщении коммита («tests-change: фикс собственной фикстуры, требования тестов не менялись») — правка своих же тестов этого MR, контракт чужих тестов не тронут.
- **Слои §3.2:** оба новых роута — только auth-гвард + вызов сервиса + рендер, бизнес-логики нет; сервис ходит в store/models/evidence_chain, против стрелки никто не импортирует.
- **Контракт store §3.5:** `store.py` в диффе не тронут; сервис использует только `find_*`; `register_*` — только в тестовом сиде. Новых `update_*`/`delete_*` нет.
- **BR-4 teacher-only:** гварды на `GET /artifacts` и `GET /artifacts/{id}/{role}` есть, оба покрыты тестами (303 → /login); модалка недоступна без сессии.
- **(а) Матрица занятий FR-4 не сломана:** контрактные тесты D1 (`test_matrix_builder.py`, `test_dashboard_matrix.py`) в диффе merge не изменены; правка `matrix.html` аддитивна (ссылка «матрица артефактов»); новые CSS-классы мягкой подсветки (`cell-found/partial/not_found`) ложатся на уже существующие классы старой матрицы и хронологии карточки — проверено по шаблонам, не мёртвый CSS.
- **(в) Взаимодействие с пакетом «12 артефактов»:** D7 собран поверх merge `da78756` (он — первый родитель). Несколько дефов одной роли (альтернативные пути, роль в двух занятиях) сведены в ОДНУ колонку на роль (`_defs_by_role_in_course_order` группирует по роли, порядок — по первому занятию); в ячейке best-wins по `SNAPSHOT_STATUS_RANK` + свежести — тест `test_cell_best_wins_between_alternative_paths` (found побеждает partial-заготовку); модалка перечисляет все альтернативные пути роли. Дублей колонок/строк нет.
- **(г) Канон точек разрыва:** модалка и свод ячейки читают ровно `{entity, quote, why}` — канон ключей из `data-model.md:146` (решение CEO 2026-07-30), эти же ключи рождает промпт `llm_client.py:53-55`. Расхождений нет.
- **(д) XSS:** Starlette `Jinja2Templates` — autoescape включён по умолчанию; `|safe` нигде в шаблонах не используется (grep по `app/templates/` пуст); студенческие цитаты (`point.quote`), заметки (`edge.notes`), пути файлов и URL репозиториев экранируются.
- **YAGNI:** лишних абстракций нет; пер-артефактный LLM-анализ в ячейке сознательно не строился (FR-11, v2) — граница зафиксирована в CHANGELOG и MEMORY.

## Прогоны (клон main `75966f1` в scratchpad, venv проекта, Python 3.13)

| Проверка | Результат |
|---|---|
| `pytest -q` (вся сюита) | **252 passed** — совпадает с tests/MAP.md. Ожидавшиеся 262 — на ветках `R-edges-8`/`T2-probes`, которые в main ещё не влиты; на main после D7 ровно 252. |
| `ruff check .` | чисто («All checks passed») |
| `pytest tests/test_artifact_matrix.py` (изолированно) | **14 passed** |
| `pytest` на tests-first коммите `7cb3680` | collection error (сервиса нет) — красная фаза TDD подтверждена |

Примечание к среде: системный Python без venv проекта даёт 57 ложных падений `[trio]`-параметризаций (`ModuleNotFoundError: trio`) — артефакт окружения, к коду отношения не имеет; с venv проекта сюита полностью зелёная.

## Рекомендации

1. Дополнить ARCHITECTURE.md (§3.1 — `artifact_matrix.py` и роуты `/artifacts`; при желании §5.3 — карта GET /artifacts) ближайшим MR — находка 1.
2. Завести GitHub Issue на D7 задним числом и сослаться из MEMORY — находка 2 (хвост уже в реестре).
3. Vendoring HTMX в `static/js` до первого офлайн-показа — известный хвост, ждёт руки CEO.
