# MEMORY — состояние проекта Course Dashboard

## Фаза (актуализация: 2026-07-30)
Требования: PRD v2.6 (FR-4 — матрица в двух видах, главный артефактный; v2.5 NFR-3/R-PD2 — ветка docs-2026-07-23 влита в D9-стек при D14). Схема данных: `data-model.md` (+MrObservation §1.14, И12). Архитектура: `ARCHITECTURE.md` v3. LLM-модель: `deepseek-v4-flash` (ADR-004 Proposed — мини-эвал собран, ждёт ключ). План: `plans/plan.md` §4 — вторая итерация, 4 волны (issues #34–#46). **В origin/main:** вся первая итерация (16 задач), FR-12 целиком (#38–#41 + порядок сдачи 29.07), фиксы #31–#33, #18 (слепая зона/хроники). Локальный клон синхронизирован 2026-07-30, смерженные ветки к удалению — команда у CEO. Пустыми остаются `coherence_analyzer` и `llm_client` — ядро FR-5 за гейтом Фазы 0 (эпик #17); скрипт снятия гейта готов (ветка C3-gate0).

## Блокеры
1. ~~FR-5a не операционализирован~~ → **закрыто 2026-07-02 (ADR-001):** ядро = LLM-агент по версионированным рубрикам; golden set в `evals/`; эксперимент 2/2 верных вердикта.
2. ~~Стек/архитектура не выбраны~~ → **закрыто 2026-07-09:** ADR-002 (стек) + ADR-003 (superseded) + `ARCHITECTURE.md` v3; LLM-модель выбрана решением CEO — `deepseek-v4-flash` (ADR-004 Proposed, гейт до Accepted — мини-эвал).
3. **Фаза 0 наполовину пройдена** — живой custdev закрыт решением CEO 2026-07-07 (CEO = единственный целевой пользователь, ценность подтверждена). Остаётся прогон рубрики FR-5 на чужих реальных репо; CSV с 4 репо студентов получен 2026-07-07 (`course/2026-07-07 …Репозитории с проектами.csv`) — доступны 2 из 4 (git.culab.ru закрыт SSO, нужен PAT).
4. ~~Два блокера модели данных~~ → **закрыто 2026-07-07:** Б1 — канон идентичности вердикта = четвёрка хешей (решение CEO), отметка FR-10 переживает реверт; Б2 — добавлены `SyncRunRepository` (+И11) и `lesson.date`; заодно закрыт С5.

## Нерешённые вопросы — единый реестр (актуализируется в начале каждой сессии)

> Правило: в начале каждой новой сессии вернуть CEO этот список. При закрытии вопроса — вычёркивать здесь же, в том же цикле.

**Требуют решения CEO:**
- ~~**ТРИ ВЕТКИ НА MERGE (2026-08-11)**~~ — **все влиты. `D43-branch-discovery`, `D44-path-candidates`, `D45-registry-archive` ушли в main ещё 11.08 (merge `4eec9d4`); пункт провисел двое суток протухшим — нашёл обход репозиториев 13.08.** В тот же обход добавлено в main: `D44-prefix-match` (третий якорь кандидатов), `S75-logging` (#75, логирование), `D41-vps` (конфигурации развёртывания, закрывает #46). Прогон после мерджей: ruff чист, **467 passed**, покрытие 96,7 % (порог 90 %). **Боевой обновлён 13.08 ≈20:35 UTC:** сервер уже стоял на `4eec9d4` с применённой `f5c8b2e91a37` и реестром из семи (импорт был сделан 11.08 — «остаётся импорт» в этом реестре тоже было протухшим); дотянут до `1065e66`, рестарт, обход `ef02d9e7` completed за 72 сек, вердикты пересчитались (llm_unavailable: 0). Новое логирование #75 в журнале видно, включая предупреждение об урезании кандидатов у С-03 (13 веток, осмотрено 5).
- **Доставка кода на боевой — git bundle, не GitHub:** origin сервера = `/opt/cd-src/cd.bundle`. Обновление = собрать бандл локально (`git bundle create ... main`), scp в `/opt/cd-src/`, `git pull` от пользователя `cd`. Старые бандлы лежат рядом с датами. `CD_DATABASE_URL` в env боевого НЕ задан — база по дефолтному относительному пути `/srv/course-dashboard/app-src/course_dashboard.db`, alembic.ini смотрит туда же (согласовано); бэкап 14.08 снят backup-API при живом WAL.
- **Авторство коммита `a712687` (ветка S75-logging, в main через merge) — «Hooks Test <hooks-test@example.com>».** Побочный эффект работы над хуками 13.08: в `.git/config` протекли `core.bare = true` и секция `[user]` с тестовой личностью. Из-за `core.bare` рабочее дерево перестало быть видно git'у (`fatal: this operation must be run in a work tree`) — обе записи сняты 13.08. Авторство самого коммита не исправлено: правка требует переписывания уже отправленной истории, действие заблокировано защитой агента. **Решение за CEO:** оставить как есть или переписать вручную (`git rebase`/`--amend --reset-author` + force-push).
- **Ревью перед merge нашло дефект в `ops/env.example`** (подробности в CHANGELOG 13.08): адрес базы был на асинхронном драйвере при синхронном движке приложения — служба стартовала бы, а первый запрос к базе падал с `MissingGreenlet`. Исправлено в том же цикле. **Проверить на боевой:** какой `CD_DATABASE_URL` реально стоит в `/etc/course-dashboard.env` — прод работает, значит там уже не то, что было в образце.
- **Три tests-change на подтверждение** (все с обоснованием в сообщениях коммитов): `test_branch_discovery.py` и `test_branch_hint_ui.py` — мои же тесты того же тикета, зелёными не бывшие (caplog не работает под фикстурой с миграциями; UTC против местного времени); `test_csv_import.py` — **настоящее изменение контракта**: ответ импорта получил поля `archived` и `restored`.
- **Вход в занятие 17 (11.08): дело защиты напрямую или общая матрица на проекторе** — практический вид открытого вопроса из фокус-групп итерации 4. С учётом того, что у четверых строки пусты не по их вине (см. обход 11.08), рекомендация — общую матрицу первой не открывать.
- **Согласие автора С-01 на живое тестирование её демо залом** (блок 2 занятия 17) — без её прямого «да» блок идёт по плану Б. Спросить до эфира.
- **Расписание до защиты (решение CEO 2026-08-11, уточнено 2026-08-13):** занятие 18 программы («Разработка с ИИ (3)») снято; 11.08 — тема 16 (пользовательское тестирование). **Темы 17 и 19 поменялись местами решением CEO 13.08:** протоколов пользовательского тестирования к 13.08 нет, разбирать на семинаре нечего, поэтому 13.08 — тема 19 (мониторинг, `course/занятие-18-мониторинг-план-и-слайды.md`), 18.08 — тема 17 (доработка сценариев по итогам тестирования) + прогон защиты и выдача допусков, 20.08 — защита. Контроль занятия 18 («инженерный прототип полного сценария») остаётся допуском к защите 18.08. Официальный документ ДОПВ не меняется.
- ~~**FIX-I2 (2026-08-04) — на merge CEO**~~ — **влито в main 2026-08-06 (merge `33c9df7`, ветка `D42-sync-busy` со стеком FIX-I2).** Остаётся у CEO: рестарт uvicorn (миграций новых нет) + «обновить сейчас» на боевой; два тикета (FIX-I2; D42); подтвердить tests-change в `tests/test_polish_d22.py`; удалить worktree `../cd-fix-i2` и копию `.env` в нём (агенту удаление запрещено). Суть дефекта — ниже, для истории.
- ~~Красный main с 30.07 (протухший тест)~~ — **снято 2026-08-11:** дефект уже устранён тикетом #49, дата считается от `utcnow()`. Полный прогон: 424 passed. Issue #72 закрыт без работы.
- ~~Мини-эвал `deepseek-v4-flash` как блокер~~ — **снято:** гейт Фазы 0 пройден 30.07, golden set 2/2, ADR-004 Accepted (CLAUDE.md проекта). Запись в этом реестре была протухшей.
- **FIX-I2 (2026-08-04) — боевой дефект ядра починен, ветка `FIX-I2-sync-commit`.** Первый обход после недельного перерыва (`731b4991`) отчитался `completed`, записал 4 новых снапшота С-03 и не посчитал ни одного вердикта — И2 «снапшоты пары не найдены» на каждую пару. Причина: `run_sync` делал `flush()`, коммит происходил позже в `get_session`, а воркеры ядра работают в собственных сессиях и незакоммиченных снапшотов не видят. Фикс — `session.commit()` перед сводом; репро-тест tests-first (красная фаза в истории), сьюта 414 → 416, ruff чист, ARCHITECTURE §5.1/CHANGELOG/MAP обновлены. **Живая петля на копии боевой БД (A/B): старый код — 6 нарушений И2 и 0 вердиктов, исправленный — 0 нарушений, 6 пар обработаны** (LLM подменён фейком, API CEO не расходовался). **От CEO:** merge **ветки `D42-sync-busy`** (содержит FIX-I2 стеком) + рестарт uvicorn (новых миграций нет) + «обновить сейчас» на боевой; завести два тикета (FIX-I2; D42); подтвердить tests-change в `tests/test_polish_d22.py` (правка фикстуры, см. ниже); удалить worktree `../cd-fix-i2` **и копию `.env` в нём** (агенту удаление запрещено правилом).
- **D42 (решение CEO 2026-08-04 «гасить кнопку») — РЕАЛИЗОВАНО, та же ветка.** `is_sync_running` — серверная истина по `SyncRun.status`: кнопка «обновить сейчас» рендерится погашенной в обеих матрицах, повторный `POST /sync` отвечает 409 вместо 500 `database is locked` (проверка ловит вторую вкладку и cron, которых браузерная блокировка не касалась). Обход, висящий `in_progress` дольше 5 минут (`ASSUME_DEAD_AFTER`, порог выбран CEO), считается следом упавшего процесса — иначе кнопка залипла бы навсегда. Тестов 416 → 424, ruff чист. **Требует утверждения CEO:** `tests-change` в `tests/test_polish_d22.py` — фикстура `_seed` теперь закрывает обход статусом `completed`; утверждения AC 5 не тронуты, но по стандарту тестирования п.3 правку тестового файла утверждает человек.
- **Пятая итерация (решения CEO «на все да», 2026-07-31) — РЕАЛИЗОВАНА ЦЕЛИКОМ, ветка C4-iter4:** спека `plans/доводки-ux-5-2026-07-31.md`. Tests-first: D23 (дедуп artifact_def миграцией `e9d23a5c1f04` — механизм выбран CEO явно; на боевой после merge: 0 дублей, вердикты целы), D24 («перемещён» + MR-события в ленте), D36 (шапка-резюме, «погашенных: 0», нейтральный /defense), D37 (русские имена ролей), C5 (bump версий 9 рубрик, golden set 2/2; боевая пересчитана по-русски — CEO нажал «обновить сейчас» 31.07, verified по БД), D38/D39 (P1+P2 пакеты), D40 (дизайн-проход по скиллу frontend-design: бумага/serif/моно, «титульный лист дела»; статусные подложки нетронуты). Сьюта 411, ruff чист; README §Статус, план §7. Первая половина (по C5) в main (merge e0145b2, конфликт MEMORY разрешён стороной ветки); **вторая половина (D38–D40) — на второй merge CEO**. **Ревью итерации 5: APPROVE по всем 8 тикетам, 0 блокеров** (1 серьёзное + 6 замечаний — закрыты доработкой 1af54e9 в той же сессии: местное время периода, «Коммит», бэкап в README, честный тест обрезки, «разрывов: N» = экрану, «перемещён» в обе стороны порядка). Сьюта 414. ~~Осталось: второй merge CEO~~ — **выполнено: вторая половина влита в main (merge `d87e9c0`, проверено 2026-08-04).** Осталось: рестарт uvicorn (миграций новых нет), тикеты GitHub (заводит CEO).
- **Фокус-группы по итерации 4 (2026-07-31, повторная проверка):** отчёт `digital-twin/staging/ux-фокус-группы-итерация-4-2026-07-31.md` — итерация принята (Оксана: 7/10 против 4/10; обзор группы ~2 мин), но 5 решений CEO: (1) чистка дубля artifact_def в боевой БД (verified: prd/`product/prd.md` ×2 в занятии 5 — след смены ключа реконсиляции; дубль в модалке + лишние LLM-пары) + guard в reconcile; (2) принудительный пересчёт вердиктов на русский до защиты 20.08 (bump рубрик) — блокирующая зависимость сцены защиты; (3) показывать ли «погашенных разрывов: 0» (сейчас пустая секция скрыта — негативный AC D19); (4) прямой вход в дело защиты без общей матрицы на проекторе; (5) состав пятой итерации (P0/P1/P2 в отчёте). Скриншоты ux2-01…07 — в scratchpad сессии (фамилии).
- **UX-фокус-группы по живым скриншотам (2026-07-31, ночь):** отчёт `digital-twin/staging/ux-фокус-группы-скриншоты-2026-07-31.md` — 3 группы по 6 скриншотам Playwright (копия боевой БД, пароль подменён; PNG в корне папки курса — фамилии, наружу не отдавать). Решения CEO: ~~(1) режим показа с псевдонимами~~ — **отклонено 2026-07-31: это названия репозиториев, не собранные фамилии; ПДн-статус адреса репо решён PRD §7 — приемлемо**; **(2) и (3) + P1/P2 — приняты CEO 2026-07-31 («остальное — да»)**: четвёртая итерация спроектирована — спеки `plans/доводки-ux-2026-07-31.md`, план §6, тикеты #64–#68. **Реализована целиком 2026-07-31 (см. «Сделано 2026-07-31, четвёртая итерация» ниже) — ветка `C4-iter4` на merge CEO.**
- ~~Главный экран дашборда~~ — **закрыт 2026-07-30: все 4 решения CEO приняты** («всё да»): (1) матрица артефактов → главный экран, матрица занятий — второй вид (требует правки PRD FR-4 → v2.6 и контрактных тестов D1 — отдельный тикет); (2) инверсия сигнала утверждена (тихие подложки + кричащий чип разрыва — осознанное отступление от сплошных заливок макета); (3) режим защиты заводится как новое требование (закрывает риск PRD §11 — US обязательна до реализации); (4) алиасы студентов (С-01…) вместо URL. Основание: `digital-twin/staging/фокус-группы-матрица-дашборда-2026-07-30.md` (3 синтетические фокус-группы). **Следующий шаг — тикеты P0 доводки D8:** MR-плашка в артефактной матрице, перенос хроник/слепой зоны/auth-баннера, честный свод (разрыв в «частично», «помечен ложным» вместо «ок»), легенда, фильтр по уверенности; P1/P2 — по отчёту.
- ~~В10~~ — **закрыт 2026-07-07:** CEO подтвердил, PRD §7 исправлен (v2.4).
- ~~Б1~~ — **закрыт 2026-07-07 (решение CEO):** канон — четвёрка хешей; FK — провенанс; отметка FR-10 переживает реверт.

**Блокеры этапов (кто/что ждёт):**
- **Сьюта зависит от интерпретатора (разобрано 2026-08-04):** прогон системным python (pytest 8.4.1 + anyio 4.10.0) даёт `414 passed, 69 failed` — фикстура `anyio_backend` параметризуется двумя движками, `[trio]`-варианты падают на импорте (пакета нет). В `.venv` проекта (pytest 9.1.1 + anyio 4.14.2) собираются только `[asyncio]` — сьюта 414 зелёная. Ни новых тестов, ни регрессии; причина — нижние границы версий в `pyproject.toml` (`pytest>=8.0`). Кандидат в тикет: явно задать `anyio_backend` или закрепить версии инструментов тестирования (правка тестов = согласование CEO). Прогонять только через `.venv/Scripts/python.exe -m pytest`. Использовано как живой кадр на слайде 12 занятия 15.
- **Развёртывание на VPS — сегодня live на занятии 15 (2026-08-04):** сервер арендован, пустой; ветка деплой-конфигов `D41-deploy` (worktree `../cd-deploy`); закрывает issue #46 и включает физически носитель FR-8 (системный cron, ARCHITECTURE §5.5). До этого продукт существовал только на ноутбуке CEO.
- Фаза 0: ~~живой custdev~~ (закрыт 2026-07-07) + прогон рубрики на чужих репо — доступны 3 репо из 4 (2026-07-09: С-03 = github.com/акк-С-03/mtb-knowledge-hub, private, читается как коллаборатор); прогон рубрики FR-5 ещё не выполнен; гейт ядра FR-5 (PRD §13). Параллельно: первая итерация частично реализована (см. «Выполненные тикеты» ниже).
- ~~Б2~~ — **закрыт 2026-07-07:** `SyncRunRepository` + `lesson.date` внесены в `data-model.md`.
- ~~Стек/архитектура/LLM-модель не выбраны~~ — **закрыто 2026-07-09:** ADR-002/003 + ARCHITECTURE v3; модель — `deepseek-v4-flash` (ADR-004).
- **Мини-эвал `deepseek-v4-flash` (C3/#34, ветка C3-gate0, 2026-07-30):** скрипт `evals/mini_eval.py` собран полным циклом (tests first → 15 тестов зелёные → ревью), 6 пар: GS-1/GS-2 + С-01 (REQUIREMENTS→DATA_MODEL, DATA_MODEL→ARCHITECTURE, ветка feat/… @ 478dec1) + С-02 (jtbd→prd, prd→ARCHITECTURE @ cea0a58), тексты закешированы. **Блокер запуска: нет `CD_DEEPSEEK_API_KEY` в .env (предусловие P2, за CEO).** Запуск: `python -m evals.mini_eval run` → отчёт в evals/ → решение CEO по ADR-004.
- ~~Переформулировать NFR-3 в PRD~~ — **закрыт 2026-07-23 (решение CEO): PRD → v2.5** (токен вне БД, env-var/файл chmod 600). Уточнение 2026-07-30: ветка docs-2026-07-23 числилась смерженной, но не была — влита в D9-artifacts-p0 при D14; в origin/main попадёт с этим стеком.
- **Красный main с 2026-07-30:** `test_mr_sync.py::test_new_approval_after_force_push_counts` — хардкод-дата 2026-07-29 против wall-clock; правка теста = изменение требования, нужен тикет с утверждением CEO (найдено пост-фактум ревью ADR-006).
- **Пост-фактум ревью ADR-006 (2026-07-30):** APPROVE с замечаниями — `reviews/ревью-пост-фактум-ADR-006-2026-07-30.md`; создан issue #48 (краш обхода на default_branch=null); ещё 3 тикета (протухающий тест; контракт store §3.5 + доки; slash-ref 404 в git_client) — на согласовании CEO.
- ~~Доступ к git.culab.ru (С-04)~~ — **закрыт 2026-07-21:** Reporter выдан, репо 11860 читается (FastAPI + React, ADR, DBML; но 0 тикетов / 0 MR, коммиты напрямую в main).
- ~~**С-01 просит права merge**~~ — **разблокировано 2026-08-06 на занятии-консультации: CEO нажал merge на MR !44** (merge-коммит `8797ceb16848`, 16:02). `main` С-01 не двигался с 16.07 — теперь там весь корпус занятий 8–11: REQUIREMENTS/DATA_MODEL/ARCHITECTURE/DESIGN/TELEGRAM_MINI_APP/FAILURES, `roles/`, `decisions/`, `test/`, `tests/`. Порядок «мерджит преподаватель» (решение 2026-07-21) в силе, права Maintainer не выданы — каждый следующий MR снова ждёт кнопки CEO; при недельном цикле это узкое место до защиты 20.08.
- **Дашборд: обход после merge С-01 не запускался.** До 06.08 матрица показывала «нет» у самой сильной работы потока — артефакты лежали в ветке, обходчик читает default branch. Фикс ядра (FIX-I2) в main, так что вердикты посчитаются. От CEO: рестарт uvicorn + «обновить сейчас» → С-01 должна впервые закрасить строку. Это же — кадр для занятия: три недели инструмент врал не по багу, а по устройству наблюдения.
- ~~Реестр студентов~~ — **закрыт 2026-07-23 (решения CEO):** С-10 и С-11 не проходят курс — исключены; 3 форка вне списка (нетронутые шаблоны) — не студенты, игнорируются. Действующий реестр: 9 человек.
- ~~Долги демо-проекта перед эфиром занятия 11~~ — **закрыты:** (1)–(4) через PR #20/#21 (смержены 21.07); (5) фамилии: MEMORY анонимизирован 21.07, **git-история переписана 2026-07-23** (решение CEO; sed-замена фамилий/аккаунтов на коды во всех .md всех коммитов; ожидает force-push CEO).
- Контракт занятия 8 на 2026-07-14 сдала только С-01 (план + 16 тикетов + MCP + итерация 1 в коде), но main пуст и issues не закрываются — её кейс анонимно в слайде «что было» занятия 9. Сводка: последний отчёт агента проверки 2026-07-14.
- **Ролевой ростер (решения CEO 2026-07-09, занятие 8):** 6 ролей — архитектор, продакт, предметный эксперт, девопс, тестировщик, финдир (YAGNI-казначей); человек в ростер не входит — валидирует каждого. Канон — файлы `roles/*.md` в репо (не субагенты: изолированы, не слышат друг друга); скилл-совещание — фасилитатор в одном контексте. **Выполнено 2026-07-09:** 6 ролей + протокол собраны в `course-dashboard/roles/` + скилл `.claude/skills/meeting/`; в project-context-template перенесены `roles/README.md`, `roles/совещание.md`, `roles/финдир.md` (пример; остальные 5 ролей студент пишет сам — часть задания занятия 8); решение оформлено курсовым ADR-004 (`digital-twin/ssot/course/decisions/ADR-004-rolevoy-roster-i-soveschanie.md`); все тексты переписаны буквальными формулировками без метафор (требование CEO); INDEX обоих репо обновлены.
- ~~Развилки митинга по упрощению архитектуры~~ — **закрыто 2026-07-09: все 5 решений CEO приняты** (модель v4-flash; триггеры на 2 таблицах; токены env-var; 9 вычёркиваний утверждены; FR-11 → v2) и внесены в документы. Протокол: `digital-twin/staging/митинг-упрощение-архитектуры-2026-07-09.md`; ревью: `reviews/08-ревью-архитектуры-занятие-7.md`.

**Серьёзные вопросы модели данных — все закрыты (`data-model.md` §6):** ~~С1~~ deferred → partial unique index + `deferred_reason` (ARCHITECTURE v2); ~~С2~~ `archived_at`; ~~С3~~ `partial_reason` JSON-массив; ~~С4~~ контент не храним (сознательно); ~~С5~~ закрыт полностью 2026-07-09 (маппинг исходов на UI — ARCHITECTURE v3 §5.3). Инварианты И1–И11 заземлены с градацией DDL/код/сид (ARCHITECTURE v3 §4).

**Открытые вопросы PRD (§12):** №7 — выгрузка Яндекс.Формы в CSV (первый ручной экспорт состоялся 2026-07-07 — запасной путь работает; автоматизация открыта); №8 — реалистичен ли порог «≤20% ложных» (замер на пилоте Фазы 0).

**Прочие вопросы модели (не блокируют, `data-model.md` §6):** В4 роль в двух занятиях, В6 credential↔repo через домен, В7 автор Override (v2+), В8 rubric_key, В9 реестр ФИО вне модели, В11 история импортов CSV (опция: `ImportRun` по аналогии с `SyncRunRepository`), В12 конфиг не версионируется, ~~В13~~ (закроется С1-решением в ADR-002).
## Работа 2026-08-11 после обхода (тикеты и код)

- **Боевой сервер проверен по SSH:** `coursedashboard.edunit.org`, служба `course-dashboard` активна, приложение в `/srv/course-dashboard/app-src` за прокси. **Планировщик работает сам:** обходы 07:00 и 19:00 UTC, `triggered_by=schedule`, шесть последних `completed` за ~70 сек. FR-8 физически включён с 04.08 — «обход не запускался» относилось к заброшенной локальной базе (она стоит с 04.08 10:49). Issue #46 закрыт по факту. Прогнан ручной обход в 15:11 после мержа MR !1 С-06.
- **Закрыты:** #46 (деплой), #63 (матрица, задним числом), #72 (протухший тест — дефекта нет), #74 (сводный ретро-тикет на FIX-I2, D42 и пятую итерацию).
- **Заведены:** #69 D43 (ветки), #70 D44 (пути), #71 D45 (архивация реестра), #73 (хвосты ревью ADR-006: контракт store §3.5 и кодирование ref).
- **Реализовано (на merge):** D43 + D44 стеком, D45 отдельно. Подробности — в CHANGELOG веток.
- **Живая петля поймала бесполезность первой редакции D43:** мера «ролей по контрактному пути» давала у ветки с 37 файлами работы тот же счёт, что у пустого шаблона. Две болезни сцеплены — кто прячет работу в ветке, тот и путей контракта не держит. После пересборки меры подсказки появились у С-05 (`dev`, 7 ролей против 4) и С-07 (`sleepy-code`, 5 против 4), ложных срабатываний у остальных пяти нет.
- **Пробел контракта артефактов:** в роли `code` не было ни одного паттерна под нативные стеки — приложение С-07 на Swift не засчитывалось как код ни в одной ветке. Добавлены `**/*.swift` и `**/*.kt`.
- **Двойная невидимость С-06:** мерж MR !1 его не проявил — работа лежит в подкаталоге, названном его логином, а в корне шаблонные заглушки, которые маскировали настоящие файлы (это и есть D44). После правки подхватятся 4 роли.
- **У С-03 остаётся нерешённое:** `develop` опережает `master` на 10 коммитов, но по ролям прироста нет — его случай не «спрятанные артефакты», а «основная ветка отстаёт». Отдельного сигнала под это не построено; кандидат в этап 1.5.

## Ручной обход всех 9 репозиториев 2026-08-11 (вне дашборда, прямые вызовы API)

Сделан перед занятием 17 по требованию CEO. Проверено по каждому репо: состав веток и даты головных коммитов, деревья файлов, MR/PR, issues, тесты, файлы развёртывания, README. Дашборд при этом не запускался — данные независимы от него.

- **Главная находка: у 4 из 9 работа лежит вне дефолтной ветки, дашборд по ним показывает пустоту.** С-03 — `develop` опережает `master` на 10 коммитов и разошёлся (T-005/006/007); С-05 — ветка `dev`, 37 файлов (PRD, спецификация расчёта, 4 персоны, 2 интервью, гипотезы, 6 макетов, свои скиллы), кода нет; С-06 — ветка `master` при default `main`, 35 файлов (концепт, PRD MVP, 3 файла валидации), кода нет; С-07 — ветка `sleepy-code`, 27 файлов, **приложение на Swift** (Xcode-проект, AppDelegate, EnergyStore, HealthExportParser, MainWindowView). История С-01 оказалась не единичной, а системной. **Заведён issue #69 (D43)** — этап 1 до 20.08: пометка «работа вне дефолтной ветки» без вердиктов, ядро FR-5 не трогает, golden set не нужен.
- **Причина системная, проверена:** в форках ЦУ `main` защищена — push и merge только Maintainer (`GET /protected_branches` по четырём форкам), студенты Developer. Слить сами они физически не могут. Инструмент, читающий только дефолтную ветку, по построению их не видит.
- ~~**С-02 — пятая неделя тишины, кода нет ни строки**~~ — **устарело, исправлено 2026-08-11: С-02 ожил.** Коммиты 10.08 (`add target example`) и 11.08 (`generate harness`, `fix mistakes`). Собран рабочий харнесс QA-агента: 4 правила, 6 скиллов со скриптами-валидаторами, 3 workflow; в `target/` — прогон на примере API с генерацией OpenAPI-клиента, тест-плана, requirements и pytest-сьюты. 88 файлов, 32 с кодом. Процесса по-прежнему нет: 1 ветка, 0 MR, 0 issues. Проект корпоративный — **UAT отдаёт коллеге** (решение CEO 2026-08-11), в аудитории не тестируется.
- **С-01 — подтверждена и встала.** 395 файлов (147 кода), 68 тестовых файлов, 53 MR (все смержены), 29 открытых issues / 50 закрытых. Публичный адрес живой, проверен снаружи: `health.json` → `status ok`, `mode mock`, ревизия `3e26751071b1` — не менялась с 06.08. Пять дней тишины.
- **С-03 — темп держит, процесс просел.** 13 PR, из них у 4 последних пустые описания; README 0 символов; тестовых файлов 0 при подключённых vitest и testing-library; репозиторий приватный → публичного адреса нет. Открытых issues 18.
- **С-04 — заморожен 37 дней** (последний коммит 04.07). При этом 121 файл, 60 с кодом, Dockerfile + docker-compose, 3 тестовых файла. Процесса нет вовсе: 1 ветка, 0 MR, 0 issues.
- **С-08 и С-09 — не начинали.** Только шаблон от 22.06; в авторах коммитов лишь `genarovv` (создание форка), своих коммитов нет ни одного ни в одной ветке.
- **Действия CEO 2026-08-11 до эфира:** написал С-03, С-05, С-07 (просьба открыть MR из своей ветки). Осталось: смержить MR !1 у С-06 (открыт с 04.07, `master` → `main`), написать С-08 и С-09 про выход на защиту, перезапустить uvicorn и прогнать обход.
- Сырые данные обхода и скрипты — в scratchpad сессии (содержат ФИО и адреса, наружу не отдавать).

## Занятие 16, 2026-08-06 (консультация вместо практикума: пришли трое из девяти)

- **Пришли С-01, С-02, С-03**; остальные шесть — нет. По программе занятие 16 = пользовательское тестирование с контролем «результаты пользовательского тестирования»; фактически проведена консультация.
- **С-01 закрыла дедлайн 6 августа по-настоящему и единственная в потоке.** Публичная веб-сборка Flutter на Firebase Hosting `https://instinctive-tails-demo-2026.web.app` (проверено снаружи: `/health.json` → status ok, mode mock, revision `3e26751071b1`) + Telegram Mini App `https://t.me/Instinctive_Tails_bot?startapp`. Демо намеренно mock: `USE_FIREBASE=false`, данные сессионные, нет Telegram-auth и общих объявлений — граница задокументирована в `TELEGRAM_MINI_APP.md`, живое подключение висит её тикетом #68. Контур: 316 Flutter + 243 rules + 112 functions, verify-all 8/8, два мутанта правил убиты. 5 августа — 8 MR (!45–!52), 6 августа — !53. **MR !44 смержен CEO в ходе консультации.** Её issues #74/#79 ждали попадания в main — теперь можно закрывать.
- **С-03 ожил и держит темп, но процесс просел под скоростью:** 7 PR с 04.08, сегодня 07:08 UTC — 3D-сцена на three.js с 2D-фолбэком по WebGL-детекту (PR #36, единственный с непустым описанием). Файлы ревью есть только для T-001…T-003 — три последние задачи прошли без ревьюера. Нет README, нет публичного адреса, тестовых файлов ноль при подключённых vitest и testing-library. Репозиторий приватный → GitHub Pages отпадает, нужен Vercel/Netlify. Открытых issues 24, среди них T-023 «Build-скрипты».
- **С-02 — пятая неделя тишины** (последний push 05.07): только документы (prd/jtbd/3 интервью/ARCHITECTURE), кода нет ни строки. До защиты 20.08 — две недели.
- **Материал для занятий:** матрица три недели показывала «нет» у сильнейшей работы, потому что артефакты жили в ветке, а обходчик читает default branch. Это не баг, а устройство наблюдения — честный кадр про то, что измеряет инструмент.

## Сделано 2026-08-04 (FIX-I2: обход коммитит наблюдения до свода)
- **Дефект найден боевым запуском CEO**, не тестами: обход `731b4991` — `completed`, 4 новых снапшота, 0 вердиктов, лог полон И2. Класс — тихий отказ: HTTP 200, матрица рисуется, ядро молча не отработало. На защите читалось бы как «у студента всё чисто».
- **Корень:** `run_sync` завершался `flush()`, коммит транзакции обхода делала зависимость `get_session` уже после ответа, а свод ставит задачи воркерам, которые открывают свои сессии (`make_verdict_worker`). Незакоммиченные снапшоты им не видны → `find_snapshot_by_id` = None → И2. Побочно: длинная транзакция обхода держит единственного писателя SQLite — параллельный POST /sync падает с `database is locked` (CEO нажал «обновить» дважды и получил 500).
- **Почему дожил до боя:** виден только при изменившемся артефакте (D28 не пишет снапшот при неизменном хеше) — прогоны 30–31.07 считали вердикты на снапшотах прошлых обходов. Тесты воркера получали закоммиченные фикстуры; сценарий «снапшот создан этим же обходом» не покрывался.
- **Сделано:** ветка `FIX-I2-sync-commit` (worktree `../cd-fix-i2`), tests-first — 2 репро-теста красными (`tests/test_sync_commit_before_reconcile.py`), затем `session.commit()` перед сводом. Сьюта 416, ruff чист; ARCHITECTURE §5.1 (шаг коммита в схеме обхода), CHANGELOG, tests/MAP.md.
- **Живая петля на копии боевой БД (A/B, LLM подменён фейком — API не расходовалось):** старый код — 6 нарушений И2, 0 вердиктов; исправленный — 0 нарушений, 6 пар обработаны (3 уникальные четвёрки). Попутный урок в копилку занятий: первый прогон петли шёл по СТАРОМУ коду — скрипт лежал вне дерева, `sys.path[0]` указывал на его папку, и `app` подтянулся из editable-установки основного дерева. Проверять, откуда загружен модуль (`module.__file__`), а не полагаться на CWD.
- **D42 (в той же ветке, стеком):** решение CEO по боевому 500 — «гасить кнопку». Сделано tests-first (7 тестов красными через ImportError, затем 8 зелёных): `is_sync_running` в sync_orchestrator, `sync_running` в обеих матрицах, 409 на роуте, `ASSUME_DEAD_AFTER = 5 мин` (порог CEO) от залипания. CSS `.sync-now:disabled` уже был — гашение читается визуально. Сьюта 424.
- **Тикеты GitHub не заведены** — создание issue заблокировано правилом внешних действий; текст выдан CEO в отчёте сессии.

## Сделано 2026-07-31 (четвёртая итерация РЕАЛИЗОВАНА: C4 + D19–D22)
- **Все 5 тикетов #64–#68 tests-first циклами** в worktree `../cd-iter4`, ветка `C4-iter4` (13 коммитов: красная фаза в истории у каждого). Сьюта 326 → 379, ruff чист.
- **C4 (#64):** промпт ядра — notes/why по-русски, entity/quote на языке документа, цитата дословно. **Гейт пройден: golden set 2/2** (прогон 2026-07-31 зафиксирован в golden-set.md; ключ DeepSeek работал из .env основного дерева — эвал запущен с CWD там, кеш пар скопирован в worktree). Реальные пары R-1…R-4 — вердикты те же, свободный текст пришёл по-русски.
- **D19 (#65):** `source_commit_date` (миграция `c7d19a4e5b02`, сбор в обходе `get_head_commit_date`, сбой → NULL); дело защиты v2 — шапка «Защита: <имя>», notes крупно, точки в details, хронология переходов (появился/статус изменился/исчез) с вехами занятий, «зафиксировано обходом» для старых снапшотов, MR-история и погашенные разрывы свёрнутыми, время ДД.ММ ЧЧ:ММ (`timeutil.fmt_dt` — Jinja-глобал).
- **D20 (#66):** сводка строки в закреплённой зоне (⚠ N, X/M, «мимо ревью: K»), сортировки «по разрывам»/«по отставанию», правая колонка «Разрывы» удалена (контракт D15 перенесён с tests-change).
- **D21 (#67):** «сдано, но разрыв» вместо «есть», чип line-clamp 2 строки + сущность ~80 симв, видимая кнопка «ложный разрыв» + «отметка обратима».
- **D22 (#68):** легенда без жаргона, переносы репо-колонки, «открыть репозиторий», «защита проекта», кнопка «обновить сейчас» (POST /sync с confirm), схлопывание пустых MR-колонок занятий, таймстемпы без микросекунд.
- **Верификация живой петлёй:** сервер :8001 из worktree на КОПИИ боевой БД (пароль подменён в копии, копия мигрирована на c7d19a4e5b02) — матрица, режим защиты С-01, матрица занятий соответствуют спеке; скриншоты в scratchpad сессии (фамилии — наружу не отдавать). Живая петля поймала мой же промах: alembic CLI мигрирует URL из alembic.ini (CD_DATABASE_URL не читает) — мигрировал случайный пустой файл `cd-iter4/course_dashboard.db` (остался, в .gitignore — удалить руками), копию домигрировал через Config API.
- **Ревью итерации агентом-ревьюером — APPROVE по всем 5 тикетам** (0 блокеров, 1 серьёзное + 8 замечаний; `reviews/ревью-итерация-4-2026-07-31.md`). Серьёзное (UTC в хронологии защиты) и 7 замечаний закрыты доработкой в той же сессии (сьюта 383); осталась только неточность слова «(красные)» в одном tests-first сообщении (2 тривиально-негативных теста были зелёными) — история неизменяема, зафиксировано здесь.
- **После merge CEO:** (1) `alembic upgrade head` на боевой БД; (2) перезапуск uvicorn (D8-урок: старый код в памяти); (3) удалить worktree `../cd-iter4` и мусорный `cd-iter4/course_dashboard.db`.

## Сделано 2026-07-30 (ночь-2: D6 + объединение связок — волны 1 и 3 закрыты)
- **D6 (#37):** deferred отличим от «проверяется» в карточке (state + причина + рендер); копилка ревью ядра закрыта (клиент на пару с aclose, валидация точек, SQL NULL проб, header без меты, displaced-пробы).
- **Решение CEO №2 по связкам:** мультифайловая сторона = объединение всех глоб-связок роли; рубрики v1.2. Ревью R/T2 — **APPROVE** после доработки (см. reviews/ревью-R-T2). Сьюта 275.
- Стек веток на merge CEO: **R-edges-8 → T2-probes → D6-deferred-ui** (D6 содержит всё). Боевая БД мигрирована на b3f1a92c7d10 (второй инцидент «не мигрирована» за день — аргумент к #46).
- **Известное ограничение (D6-4, риск на заметку):** union-список путей НЕ входит в четвёрку кэша — изменение functions-связки не инвалидирует вердикт lib-пары (LLM-вход изменился, вердикт остался); плюс кросс-произведение × union = несколько идентичных LLM-вызовов на роль. Следствие комбинации двух решений CEO 2026-07-30; триггер пересмотра — расхождение вердиктов пар одной роли или рост счёта API.
- Пробы на живых репо: у ВСЕХ 9 студентов нет стандартов документации/тестирования/отладки в CLAUDE.md; у 6 — шаблонная секция «Уточняющие вопросы» в ARCHITECTURE. Слайд для занятия 14.

## Сделано 2026-07-30 (вечер-2: рёбра 2→9, пробы, аудит D7)
- **R (#42):** 6 новых рубрик v1.0 утверждены CEO; боевой прогон — 17 пар, 9 новых вердиктов С-01 (USER_JOURNEY→REQUIREMENTS ok 23 сущностей; ARCHITECTURE→план break: потеряны Media moderation и Audit writer). **Два решения CEO:** мультифайловая сторона пары = список путей связки (рубрики plan→code и code→tests подняты до v1.1 — старые ложные break по «представителю» уйдут пересчётом четвёрки); кросс-произведение альтернатив роли оставить (обе проверяются).
- **T2 (#44):** content_probes узкой редакцией — конфиг проб (валидация «ровно одно условие»), findings в снапшоте (миграция b3f1a92c7d10), D28, BR-3 нетронут, блок в карточке; пробы: стандарты документации/тестирования/отладки в CLAUDE.md, «Стек: <», «Уточняющие вопросы» в ARCHITECTURE.
- **Аудит D7 (работа параллельной сессии, по просьбе CEO):** «замечания, серьёзных нет» — TDD честный, слои/BR-4/XSS чисто, канон точек совпадает; хвосты закрыты этой сессией: ARCHITECTURE §3.1 дополнен (artifact_matrix.py, GET /artifacts), тикет D7 заведён задним числом. HTMX CDN — остаётся (vendoring руками CEO).
- Ревью R/T2: REWORK (блокеры документационные) → закрыты этим же коммитом (data-model: content_probes/probe_findings; golden-set: раздел рёбер с кандидатами и вейвером CEO; MAP; CHANGELOG).
## Сделано 2026-07-31 (третья итерация ЗАКРЫТА: D15–D18, P1 + режим защиты)
- **Решение CEO «всё утверждено»:** P1 (#59–#61) и US-C2/режим защиты (#62) — реализованы tests-first циклами в worktree `../cd-iter3`, ветка `D15-p1`. Сьюта 314 → 326, ruff чист.
- **D15:** счётчик уникальных разрывов + `?sort=breaks` (живёт в URL, переживает POST FR-10) + sticky шапка/колонка. **D16:** метки свежести по `computed_at`/`observed_at` последнего обхода (D25 не мигает; первый обход без меток; +`find_previous_sync_run`). **D17:** бейдж уверенности, якорь из модалки в карточку (+id рёбер), фокус-менеджмент. **D18:** `GET /students/{id}/defense` — только high+не погашенные разрывы, даты и коммиты обеих сторон (+source/target file/sha в карточке ребра), без FR-10-кнопок, «разрывов для показа нет», слепая зона — пометка; входы из матрицы и карточки.
- **Канон:** US-C2 внесена в user-stories (+строка трассировки), PRD → v2.7 (FR-13; риск §11 → «реализовано»), README/CHANGELOG/MAP/plan §5 обновлены.
- **Влито и запушено (origin/main 553f2c9):** D15-p1 + fr11-pilot (#45 параллельной сессии) + правило worktree в CLAUDE.md; ветки и worktree удалены; тикеты #59–#62 закрыты по «closes».

## Сделано 2026-07-30 (P0 третьей итерации РЕАЛИЗОВАН: D9–D14, артефактная матрица — главный экран)
- **D9–D13 (#53–#57) tests-first циклами:** MR-плашка; сигнальные блоки (blind_spots_and_signals общая) + stale >48ч; честный свод (разрыв в «частично», «помечен ложным»); инверсия сигнала + чип уверенности; легенда. Сьюта 256 → 280, ruff чист.
- **D14 (#58):** `/` — артефактная матрица (алиас /artifacts), занятия — `/lessons`; PRD v2.6 (FR-4 два вида, §2), US-B1 уточнена; контрактные тесты → /lessons (tests-change по решению CEO).
- **Находка:** ветка docs-2026-07-23 (PRD v2.5 + закрытия реестра 23.07) не была смержена вопреки записи в MEMORY — влита в D9-artifacts-p0, реестр сверен. ~~Порядок merge для CEO~~ — **выполнено 2026-07-30:** весь стек дня (D6/R/T2 + D8 + D9) влит в origin/main (47432f7; 305 тестов, покрытие 97.9%); 6 веток и 2 worktree удалены.
- Осталось из итерации: P1 — D15 (#59) sticky/счётчик/сортировка, D16 (#60) свежесть, D17 (#61) модалка; P2 — D18 (#62) режим защиты (US-C2 на утверждение CEO).

## Сделано 2026-07-30 (третья итерация спроектирована: доводки артефактной матрицы)
- **Решения CEO по фокус-группам** («всё да», затем уточнение): артефактная матрица → главный экран; инверсия сигнала утверждена; режим защиты — новое требование (черновик US-C2 готов, утвердить до реализации); **алиасы отменены — остаются ссылки на репозитории** (короткое имя + URL в title, как в D8, решению не противоречит).
- **Спеки D9–D18 в логике канона** (трассировка FR/US/BR, Given/When/Then, негативные сценарии, документы): `plans/доводки-артефактной-матрицы-2026-07-30.md`; порядок и зависимости — `plans/plan.md` §5. Ветка P0: `D9-artifacts-p0` (стек на D8-matrix-rus), worktree `../cd-d9`.
- **Тикеты созданы: #53–#62** (D9–D18) — номера внесены в plan.md §5.
- Инцидент параллельной работы повторился: рабочее дерево переключено другой сессией (R-edges-8) посреди работы — спеки уехали в чужое дерево, перенесены в worktree cd-d9, чужое дерево очищено от моих файлов. Правило-кандидат: каждая сессия — свой worktree.

## Сделано 2026-07-30 (D8: матрица занятий приведена к макету — решения CEO из сверки с maket 1.png)
- **Ветка `D8-matrix-rus`** (стек на D7-artifact-matrix), полный цикл: tests first (4 красных) → реализация → suite 256 зелёных, ruff чист. Решения CEO этой сессии: (1) две матрицы — ок; (2) цвета оставить как в app.css; (3) статусы/причины/уверенность по-русски (есть/частично/нет; общие ярлыки `services/labels.py` → globals Jinja, модалка D7 переведена на них же); (4) репозиторий в строке — хвост URL, полный адрес в title; (5) «Актуально на» + дата ДД.ММ (обе матрицы, единая формулировка); (6) колонки «Разрывы связности» и «Процесс» — вторым блоком под матрицей (только строки, где есть что смотреть). Правки контрактных тестов D1/#32 (template_copy→по-русски, as_of с датой) — изменение требований, согласовано CEO, гейт H1 пройден строкой tests-change.
- **Верификация живой петлёй:** сервер из worktree на :8001, копия боевой БД (пароль админа в копии заменён на временный), Playwright-скриншоты обеих матриц — соответствуют макету.
- **Инцидент параллельной работы:** основная рабочая копия во время сессии ушла на ветку `demo/doc-gate-test` (демо занятия 14) — amend упал на смене HEAD; работа D8 перенесена в worktree `../cd-D8` (stash поднят, ничего не потеряно). Не работать в основной копии, пока идёт демо.
- **Хвосты D8 (на решение CEO):** (1) сервер на :8000 крутит код до D7 — /artifacts отдаёт 404, после merge нужен перезапуск uvicorn; (2) тикеты GitHub на D7 и D8 не заведены — завести и сослаться; (3) верификационный сервер :8001 остановить после приёмки; (4) порядок merge: T3-12-artefacts → D7-artifact-matrix → D8-matrix-rus.

## Сделано 2026-07-30 (D7: матрица «репозиторий × артефакт» по макету CEO)
- **Ветка `D7-artifact-matrix`** (стек на T3-12-artefacts — нужны 6 новых ролей): tests first (14 тестов, красная фаза в истории) → реализация → suite 252 зелёных (включая ранее красный test_mr_sync — хардкод-дата 2026-07-29 сегодня прошла: тест протухает волнами, тикет всё ещё нужен), ruff чист. По макету `maket 1.png`: GET /artifacts — строки-репо × колонки-роли (порядок — по первому занятию роли), ячейки зелёный/оранжевый/красный/серый с усечённым сводом («частично · заготовка из шаблона», «есть · разрыв: потеряна „X‟»), клик (HTMX) → модалка: файлы роли, рёбра с точками-цитатами и заметками, кнопка FR-10. Старая матрица занятий (FR-4, контрактные тесты) не тронута — взаимные ссылки.
- Проверено живой петлёй: временная БД с сидом из config.yaml (15 ролей реального конфига), сервер + httpx-смоук — все проверки ок. Скриншот-петля Playwright не сработала: профиль mcp-chrome занят чужим окном браузера (закрыть перед эфиром!).
- **Скоуп-границы (сообщено CEO):** пер-артефактный LLM-анализ в ячейке = FR-11 (v2, решение 2026-07-09/28 в силе) — в ячейке наличие FR-4 + свод рёбер FR-5. Макет противоречит FR-4 буквой («репозиторий × занятие») — снято дополнительным видом, не заменой; если /artifacts должен стать главным экраном — нужно решение CEO (PRD v2.6 + правка контрактных тестов D1).
- **Хвосты D7 (на решение CEO):** (1) HTMX подключён CDN-ом — vendoring в static/js руками (сеть из сессии заблокирована); (2) тикет в GitHub Issues на D7 не создан — завести и сослаться; (3) merge после T3-12-artefacts.

## Сделано 2026-07-30 (предэфирная механика занятия 14)
- **Проверка репо 30.07** (`digital-twin/staging/проверка-репозиториев-студентов-2026-07-30.md`): живой 1 из 9 (С-01: ~13 коммитов, MR !42 tests-first с красной фазой и мутациями; работа сделана до эфира 13); маркер «Причина/Доказательство» и «bug repro» — ни у кого (окно полтора дня); полевой журнал — ни у кого, у С-01 свой FAILURES.md на 19 записей (по духу журнал); MR к кнопке merge — нет. Слайд 2 занятия 14 заполнен. Новый пункт реестра: запрос С-01 о правах merge (см. выше).
- **Ветка-страховка `demo14-ui` готова** (план Б блока 1): worktree `../cd-demo14`, 2 коммита (tokens.css + подключение; app.css 448 строк по всем классам шаблонов + DESIGN.md, 5 правил); ruff чист, pytest 228 зелёных; не запушена. Попутный кейс: Write-инструмент заблокировал создание tokens.css как «credentials» — ложное срабатывание замка на имя файла (копилка занятия).
- В сценарий блока 4 добавлен свежий студенческий кейс (анонимно): tests-first коммит против собственного pre-commit гейта С-01 → узкое исключение; студент сам назвал это «машинный замок».

## Сделано 2026-07-30 (занятие 14 спроектировано)
- **Ресёрч (4 агента):** `digital-twin/staging/ресёрч-занятие-14-2026-07-30.md` — внутренний аудит edunit (формат занятий, обещания про хуки, эталоны ревью/фронтенда, 10 живых кейсов) + фронтенд-практики (image-to-code, Playwright-петля, frontend-design skill, DESIGN.md) + ревью (verified vs plausible, severity, дефекты ИИ-кода) + справка hooks Claude Code.
- **Решения CEO:** безопасность ИИ-кода (обязательство §«Знает») вплетена в блок ревью; перекрёстное ревью однокурсников не берём; полевой эксперимент — сразу план Б (наши кейсы как спецификация замков).
- **План + слайды:** `course/занятие-14-ревью-замки-интерфейс-план-и-слайды.md` (версия 1) — 4 блока: макет→интерфейс (D6, голая матрица 37 классов/2 стиля) / визуальная петля + дизайн-система / ревью следующего уровня + безопасность / машинные замки (live: safety-gate на `git branch -D`, commit-msg гейт). Шпаргалка: `course/занятие-14-команды-эфира.md`.
- **Решение CEO: канонизации путей не будет** — дашборд проверяет только 12 артефактов из описания курса; полевой журнал — рабочая практика студента без роли в дашборде; T3 в части field_journal/test_map не заводится (пункт plan.md про «после канонизации путей на занятии 14» снят).
- **Методическое ревью проведено** («годно с правками», 0 блокеров, 7 серьёзных + 7 замечаний + 2 мелочи): правки внесены версией 2 в тот же день; конспект — `digital-twin/staging/ревью-методиста-занятие-14-2026-07-30.md`. Вопросы ревью решены CEO в тот же день: В1 — З1–З2 ядро недели, З3–З4 второй эшелон; В2 — маркера недели нет (осознанно); В3 — тезис 47 закрыт строкой на слайде 8.
- **Версия 3: адаптация под opencode** (напоминание CEO: эфиры ведутся в opencode). Ресёрч по opencode.ai/docs: изображения работают только с моделью `attachment: true` — Qwen3-Coder картинки не принимает (нужна Qwen3-VL или аналог); MCP и permission — в opencode.json; скиллы opencode читает из `.claude/skills/` напрямую; ревьюер переносится с правкой frontmatter. Замок №1 демо = permission-правило `"git branch -D *": "deny"`; формулировки заданий tool-agnostic. Vision-вопрос закрыт решением CEO: блоки 1–2 эфира — в Claude Code (opencode-модель Qwen3-235B/Yandex текстовая, DeepSeek API тоже без vision); студентам на слайде 5 — рекомендация VLM (Qwen3-VL/Qwen2.5-VL). Блок 4 — opencode permission. **До эфира: вынести ключ Yandex из ~/.config/opencode/opencode.json в env** (лежит плейнтекстом, файл показывается в блоке 4). План готов к эфиру, осталась предэфирная механика («До эфира» п.1–9).
- **До эфира:** свежая проверка репо для слайда 2; merge C2-coherence + зелёный pytest; фото макета; Playwright MCP + скилл frontend-design + перезапуск; ветка-страховка demo14-ui.

## Сделано 2026-07-30 (поздняя ночь: ПЕРВЫЕ БОЕВЫЕ ВЕРДИКТЫ)
- **Пакет «12 артефактов» (решение CEO, по ручному аудиту С-01: 10/12 артефактов есть — конфиг видел 1):** +6 ролей enum (jtbd/readme/changelog/adr/claude_md/roles_roster), альтернативные пути на роль, мультистековые code/tests, занятия 2 и 10, ребро prd→architecture (рубрика v1.0). Ветка `T3-12-artefacts`, на ревью.
- **Два дефекта, найденные боевыми прогонами, починены tests-first:** (1) ключ artifact_def был (занятие, роль) — альтернативы затирались; стал (занятие, роль, паттерн); (2) агрегация ячейки наказывала partial за отсутствие альтернативного пути — теперь двухуровневая (best-wins внутри роли). Сьюта 237.
- **Боевая БД мигрирована** (не была после FR-12 — кейс для занятия 15; бэкап course_dashboard.db.bak-2026-07-30).
- **Обход №3 — 7 боевых вердиктов:** С-01 — 5×ok (0 потерь, работа связна); С-02 — break 15/15 потеряно (PRD→архитектура: пустой шаблон, точки с цитатами); С-03 — break 9+2 (тонкий разрыв: потеряны «масштаб и потоки», «жизненный цикл»). Петля ценности PRD замкнута: «найди мне разрывы» работает на живых данных.
- Известное ограничение: при двух found-альтернативах одной роли пары дублируются (кросс-произведение) — в D6/#37.

## Сделано 2026-07-30 (ночь: ЯДРО FR-5 РЕАЛИЗОВАНО)
- **CEO смержил integrate-2026-07-30 → main → push** (212 тестов; issues #34/#48–#51 закрыты «Closes»).
- **C1 (#35) llm_client** — tests first → 11 тестов → check_coherence + validate_llm_response §5.2, промпт-каркас v1 канонически в клиенте; мини-эвал переведён на клиента (AC: вердикты идентичны прямому прогону).
- **C2 (#36) coherence_analyzer** — tests first → 13 тестов → ensure_verdict (D25, И2, деградации; git-сбой = None без записи), make_verdict_worker (сессия на пару + Lock), проводка /sync (без ключа воркер None); +4 find_* в store. Сьюта 223, ruff чист.
- **Ревью ядра: REWORK → доработка → C1 APPROVE + C2 APPROVE** (reviews/ревью-C1-C2-ядро-2026-07-30.md). Блокеры сняты: канон points {entity, quote, why} (решение CEO) проведён везде + перепрогон golden set 2/2; контрактные тесты восстановлены; model из четвёрки; И2-лог. Сьюта 228.
- **R-3 (С-02 jtbd→prd) нестабильна** (ok→break при смене ключей промпта, golden set стабилен) — кандидат на эталонирование, нужна ручная сверка преподавателем.
- Мелочи ревью в D6/#37: мёртвый фолбэк `or repo.default_branch` в ensure_verdict; LLMClient не закрывается на /sync; валидатор не проверяет ключи элементов points.
- **Ветка на merge CEO: `C2-coherence`** (содержит C1; C1-llm-client-v2 — префикс). Ревью C1+C2 запущено. Устаревшие локальные `C1-llm-client` (старая, на C3) — удалить.
- Дальше по плану волны 1: D6 (#37, deferred в UI) → первый боевой обход с вердиктами; волна 3 — рёбра 2→8 (#42), T3 (#43), T2 (#44).

## Сделано 2026-07-30 (вечер: ГЕЙТ ФАЗЫ 0 СНЯТ)
- **Мини-эвал прогнан вживую** (ключ CEO добавлен; алиасы DEEPSEEK_API_KEY/DEEPSEEK_API_BASE_URL без CD_ поддержаны, base_url посредника уважается): **golden set 2/2** (GS-1 ok без ложной «слепой зоны», GS-2 break с эталонной точкой про Оксану), реальные пары — 3 ok + 1 честный break (С-02 prd→ARCHITECTURE: пустой шаблон). Отчёт: `evals/отчёт-мини-эвал-2026-07-30.md` (локальный, не в git — цитаты из закрытого репо С-01).
- **Решение CEO: ADR-004 Accepted, гейт Фазы 0 снят, ядро разблокировано** — прогоны зафиксированы в golden-set.md с llm_model. Дальше: C1 llm_client → C2 coherence_analyzer (ветки стеком на C3-gate0-clean — ей нужны алиасы ключа).
- Ревью fix-веток: **#49 APPROVE, #48 APPROVE, #50 REWORK→доработан** (8 мест «4→5 update_*» + шаг branch_detect в схеме §5.1).

## Сделано 2026-07-30 (сессия тикетов)
- **Мини-эвал C3 (#34) собран** полным циклом (tests first → ревью REWORK → доработка → APPROVE): ветка `C3-gate0-clean`, 6 пар, кеш текстов и маппинг репо — вне git (блокер деанонимизации снят, история чистая). Ждёт `CD_DEEPSEEK_API_KEY` (P2, CEO) → `python -m evals.mini_eval run`.
- **H1 (#52) хуки/гейты** к занятию 14: ветка `H1-hooks` (ревью REWORK → доработка: execute-бит, границы гейта в README, MAP 184). Гейт commit-msg дважды сработал вживую в этой же сессии — готовый кейс занятия.
- **#49/#48/#50** реализованы tests-first циклами: детерминизация протухшего теста (`fix49-test-rot`); guard default_branch=null (`fix48-null-default`); контракт store 4→5 update_* + `branch_detect.py` (`fix50-store-contract`, стек на fix48). Ревью трёх веток запущено.
- **#51** slash-ref в git_client (`fix51-slash-ref`): quote(ref, safe='') + 2 теста.
- **Порядок merge для CEO:** fix49 → H1-hooks (pre-push требует зелёной suite); fix48 → fix50 (стек); fix51; C3-gate0-clean. Старые `C3-gate0` и `diversant-plan-b` удалить принудительно, не пушить.

## Сделано 2026-07-28 (эфир занятия 13)
- **Первый боевой запуск продукта:** локальный сервер, боевая БД, импорт 9 реальных репо (8 доступно / 1 слепая зона), первый обход live на эфире — status partial, матрица с реальными данными показана залу. Детект заготовок сработал на 6 форках (template_copy) + 1 wrong_place.
- **Живой баг проведён по полному циклу отладки на эфире:** симптом — живой репо С-03 в слепой зоне (HTTP 404); причина — код в `master`, обходчик регистрировал все репо с `default_branch=main` (класс «пропущенный краевой случай»); фикс — ADR-006 (коммит 14e68fd): детект default-ветки при импорте + сверка при обходе для старых записей; репро-тест `test_repro_404_mtb_knowledge_hub.py`. Второй учебный момент эфира: после фикса сервер держал старый код в памяти (uvicorn без --reload) — «починено в коде» ≠ «починено в работающей системе».
- **Хвосты (актуализация 2026-07-30):** (1) ~~ревью ADR-006~~ — сделано, APPROVE с замечаниями (см. реестр); (2) ~~диверсант~~ — worktree удалён по решению CEO, ветку добить `git branch -D`; (3) ~~тикет Settings~~ — закрыт (#33 смержен, extra=ignore); (4) cron/деплой на VPS — открыт, issue #46 (волна 4, материал занятия 15).

## Новые пункты реестра (2026-07-28, вечер — перед запуском обходчика)
- **Обходчик vs порядок сдачи — системный разрыв:** порядок сдачи занятия 11 («работа = принятый MR, кнопка у преподавателя») сделал main пустым by design, а обходчик v1 читает только default branch → матрица покажет «нет» у честно работающих (у С-01 10 принятых MR живут в долгоживущей ветке). Плюс маркеры недель (tests first, «Мутация», «Причина/Доказательство») обходчик не видит вовсе — он читает деревья файлов, не историю коммитов и не MR. **Черновики тикетов заменены полным гэп-анализом** (2026-07-28, поздний вечер): `digital-twin/staging/гэп-анализ-задания-vs-дашборд-2026-07-28.md` — задания занятий 3–13 × ДОПВ × PRD × код; предложены T1 (FR-12: наблюдение MR/коммитов — закрывает порядок сдачи занятия 11 и все маркеры недель), T2 (пробы содержимого в конфиге — довыполнение FR-4), T3 (новые роли артефактов), T5 (проверка URL прототипа к 6.08), дополнения PRD и config.yaml (рёбра 2→8), правки курса (канонизировать пути полевого журнала и карты тестов). До тикетов проверка маркеров — вручную агентом.
- ~~С-02: репозиторий не найден~~ — **закрыт в тот же вечер:** в маппинге имя репо было записано как имя аккаунта; URL найден в исходном CSV от 07.07, маппинг исправлен. Статус С-02: тишина с 05.07 (доки без кода, 0 PR).
- С-10/С-11 исключены решением CEO 2026-07-23 — реестр 9 студентов; старый пункт «репо не найдены» закрыт.

## Новый пункт реестра (2026-07-28)
- ~~Смержить стек первой итерации~~ — **закрыт: CEO смержил всё в origin/main (проверено 2026-07-30), issues первой итерации закрыты.** Осталось у CEO: удалить смерженные локальные ветки (`git branch --merged origin/main | grep -vE "\*|^..main$" | xargs git branch -d`) и `git branch -D diversant-plan-b` (worktree cd-diversant удалён 2026-07-30 по решению CEO; принудительное удаление ветки в сессии заблокировано хуком).

## Сделано 2026-07-28
- **Все 8 тикетов первой итерации реализованы за одну сессию** (директива CEO «реализуй все нереализованные тикеты»): D1 #12 (matrix.html + GET /, детерминированная агрегация ячейки при нескольких артефактах), S4 #6 (config.yaml + config_manager по ADR-005: реконсиляция, repoint рубрик со старыми вердиктами нетронутыми, ограничитель «только config_manager» тестом на импорт, fail-fast без конфига, чтение на старте + POST /admin/reload-config), G2 #9 (sync_orchestrator: обход, классификация, инкрементальность D28, все 5 исходов + detail, source_commit_sha для FR-9, POST /sync с X-Sync-Token), G3 #10 (template_copy: хеши шаблона раз за обход, адрес шаблона в config.yaml по PRD FR-4, пустой файл = заготовка, wrong_place по BR-3 без залипания), G4 #11 (идемпотентный свод-реконсиляция пар; воркер вердиктов инъецируется — ядро FR-5 не кодилось), I2 #13 (/health со счётчиками из БД + crontab в README §Деплой), D4 #14 (evidence_chain + карточка студента: хронология по observed_at, 2 ребра, ≤5 точек), O2 #16 (toggle «ложный разрыв» в матрице и карточке; отметка гасит подсветку, новая четвёрка её не наследует).
- **Процесс:** каждый тикет — ветка с ID, коммит «tests first» с видимой красной фазой, реализация, ревью агентом-ревьюером (D1, G2, G3 — через REWORK с доработкой; S4, G4, I2, D4, O2 — APPROVE с minor-фиксами). Итог: 120 тестов (было 36), покрытие 98%, ruff чистый.
- **Попутные фиксы:** alembic/env.py — `fileConfig(disable_existing_loggers=False)` (fileConfig глушил логгеры приложения); тест `/health` из test_app_starts заменён suite'ом test_health.py (контракт изменён тикетом #13); стек ребейзнут на main поверх PR #20/#21 (1 конфликт в README, разрешён объединением).
- Скоуп-решения зафиксированы комментариями в issue #10 (wrong_place — в G3; inexact_name — вне, нет правила в PRD; мульти-файловые наблюдения с шаблоном не сравниваются).

## Сделано 2026-07-23 (подготовка занятия 12)
- **Занятие 12 спроектировано:** `course/занятие-12-рефакторинг-и-зависимости-план-и-слайды.md` — тезисы 40–42; блок 1 = долг занятия 11 (диверсант + стандарт тестирования; на эфире 11 успели Зблоки 1–2, З3–З4 перенесены — подтверждено CEO); блок 2 = рефакторинг matrix_builder под тестами (кадр боли: прод-инцидент hextutor EDU-740 этой недели + данные GitClear/METR); блок 3 = безопасность зависимостей live (pip-audit, slopsquatting 19,7%, кадр боли EDU-719 aiohttp/aiogram); блок 4 = параллельная оркестрация (обещана на зан. 10): два opencode в двух worktree на тикеты #6 и #9, слияние с разбором конфликта. Все развилки решены CEO вопросами в сессии.
- **Проверка 9 реп (2026-07-23):** маркеры недели «Тесты» строго 0 из 9 (по сути 1 — С-01 делает мутации, но после кода и без строки-маркера; self-merge продолжается); С-03 ожил (архитектура, вне процесса); 7 без активности с 16.07 и раньше. Подача на слайде 2 — мягкая (решение CEO). Отчёт: `digital-twin/staging/проверка-репозиториев-студентов-2026-07-23.md`.
- **PRD → v2.5:** NFR-3/R-PD2 переписаны (решение CEO).
- **Git-история переписана** (решение CEO): фамилии и аккаунты → коды С-xx во всех .md всех коммитов (filter-branch + sed); бэкап-зеркало в scratchpad; ожидает force-push CEO; полная очистка кэша GitHub — по запросу в поддержку (старые SHA могут оставаться в кэше).
- **D1 (#12) дооформлен:** tests/MAP.md закоммичен в ветку, `.coverage` в .gitignore; ветка готова к PR.

## Сделано 2026-07-21 (поздний вечер)
- **Методическое ревью занятия 11** (агент-методист, чистый контекст): 4 блокера (домашка невыполнима для большинства; контроль противоречил порядку сдачи; З3 не проверялась; блок 4 перегружен) + 12 предпосылок дизайна. 11 предпосылок вынесены CEO вопросами — все решены. Конспект + реестр решений: `digital-twin/staging/ревью-методиста-занятие-11-2026-07-21.md`.
- **Занятие 11 пересобрано (версия 2)** по решениям CEO: единая планка; честные глаголы («организует/принимает»); приёмка тестов в 2 версиях (код / таблица «тест → требование»); кадр боли до правил; мутации — второй агент-диверсант (идеал «человек + агент» проговаривается); окно до хуков = полевой эксперимент к занятию 14; контроль недели — 2 машинно-проверяемых маркера (коммит «tests first» раньше кода в принятом MR + строка «Мутация: … — Поймал: …» в описании MR), «наличие tests/» убрано; команды покрытия для стеков; тесты позиционированы как обязательный артефакт портфолио + ускоритель к 6.08.
- **Драфт сообщения молчащим студентам** (3 варианта: молчащие / С-04 / С-10+С-11): `digital-twin/staging/сообщение-молчащим-студентам-2026-07-21-draft.md` — **на согласование CEO, отправить до занятия 12**.
- **Гэп-анализ hextutor vs best practices** (по запросу CEO): не применяются — мутационное тестирование, техническая защита тестов (хуки), coverage-гейт в CI (fail_under нет, интеграционные с --no-cov), property-based, политика флаки, evals качества LLM-ответов. Усиливает нарратив «политика в тексте ≠ машинный гейт» для занятий 14–15.

## Новые пункты реестра (2026-07-21)
- ~~Отправить сообщение молчащим~~ — **закрыт 2026-07-28 решением CEO: не отправляем** (дедлайнов в курсе нет, зачёт по другим критериям; сигнал отставшим — слайд 2 занятия 13 с коротким входом). Драфт в staging остаётся архивом.
- ~~К занятию 14: настроить хуки/гейты заранее + собрать кейсы полевого эксперимента~~ — **закрыт 2026-07-30:** хуки H1 (#52) в main; кейсы — решение CEO: план Б (кейсы демо-проекта) как основной, записи студентов поверх.
- **Дашборд должен уметь проверять маркеры контроля недели 6:** коммит «tests first» раньше кода в принятом MR; строка «Мутация: …» в описании MR — учесть в рубриках FR-5/обходчике FR-3.

## Сделано 2026-07-21 (вечер)
- **Права merge в ЦУ — загадка «работы без слияния» разгадана:** в форках culab ветки main/ci защищены (merge/push только Maintainer 40+), студенты в своих форках — Developer (30): физически не могли ни смержить, ни снять защиту (проверено: акк-С-01=30, акк-С-05=30; v.genarov=Owner 50). **Решение CEO 2026-07-21: настройки не менять, мерджит преподаватель** — новый порядок сдачи «MR, принятый ревьюером студента, = сдан» внесён в слайд 2 и домашку занятия 11. Ретро-переоценка: часть вердиктов «движение без слияния» в проверках 07–16.07 была несправедлива к студентам.
- **Три дискавери для занятия 11:** (1) тестовая база hextutor — 588 файлов/~7 240 тестов, «голые юнит-тесты» подтверждены прод-инцидентами (фантомный `RedisClient.setex` при 130 зелёных тестах, тесты-слепки спрятали 5 дефектов, skip вместо починки) → `staging/дискавери-тесты-hextutor-2026-07-21.md`; (2) память/доки hextutor — эволюция политики (real-adapter → intent-дисциплина → hard-block гейт TEST-INTENT), корень тавтологий — собственный плейбук тест-агентов «тест матчит main» → `staging/дискавери-память-тестов-hextutor-2026-07-21.md`; (3) ресёрч best practices AI-native тестирования (AgentCoder, reward hacking, mutation testing у Google/Meta, protected tests хуком, diff coverage) → `staging/ресёрч-автотесты-ai-native-2026-07-21.md`. Кейсы вплетены в блоки 3–4 и слайды 7–8 занятия 11.

## Сделано 2026-07-21
- **Проверка 11 реп студентов перед занятием 11:** полный цикл — 1 из 11 (С-01: 44 закрытых тикета, 39 смерженных MR за 5 дней, README + стандарт документации; оговорка — интеграционная ветка вместо main, последние 17 MR без ревью); С-04 открылся (Reporter выдан) — «main без процесса» (0 тикетов/MR); остальные 8 — без коммитов за неделю; С-10/С-11 не найдены (3-я проверка). Отчёт: `digital-twin/staging/проверка-репозиториев-студентов-2026-07-21.md`.
- **Аудит демо-проекта vs ДЗ занятия 10:** main запушен (7 тикетов, pytest 42 passed в дереве O1, ruff чистый), но 5 долгов до эфира (доку-фиксы не закоммичены, #15 не слит, З3 не выполнен, TEMP-файл, фамилии в MEMORY) — внесены в реестр выше.
- **Занятие 11 спроектировано:** `course/занятие-11-тестирование-план-и-слайды.md` — тезисы 37–39, контроль «Тесты»; 4 блока: карта тестов + pytest-cov → test-first на #6 (S4, контракт ADR-005) → мутационная отбраковка сгенерированных тестов → тесты как спецификация + стандарт тестирования в CLAUDE.md/чек-лист ревьюера; фоновый оркестратор на #9 в режиме test-first; слайд 2 заполнен данными проверки 21.07.

## Сделано 2026-07-09
- **Ревью архитектуры занятия 7** (агент-архитектор, чистый контекст): 14 находок (1 блокер — модель ядра не выбрана; 7 серьёзных; 6 мелочей). Заархивировано: `reviews/08-ревью-архитектуры-занятие-7.md`.
- **Митинг упрощения архитектуры** (6 персон, линза «вычеркнуть, а не добавить»): 8 из 14 находок закрыты удалением/упрощением; ресёрч цен DeepSeek (v4-flash ≈ $1.3–1.5/мес по расчёту). Протокол: `digital-twin/staging/митинг-упрощение-архитектуры-2026-07-09.md`.
- **5 решений CEO:** модель ядра `deepseek-v4-flash` (pro — fallback после пилота Фазы 0); append-only триггеры только на `artifact_snapshot`+`coherence_verdict`; токены — env-var/chmod 600, крипто из БД убрано; все 9 вычёркиваний утверждены (каждое — с записью «почему не строим»); FR-11/StepQualityCard — файл модели в v1-коде не создаётся.
- **Правки документов внесены:** ARCHITECTURE.md → v3 (свод-реконсиляция вместо дельты, cron — носитель FR-8, журнал/состояние §3.5, градация инвариантов, маппинг исходов→UI, пометка гейта Фазы 0 на `coherence_analyzer`); `data-model.md` (И5 без SyncRun, кеш-колонки вычислимые, GitCredential без `encrypted_token`, И8/И10, С5/В13 закрыты); INDEX.md синхронизирован; ADR-002 — честные плюсы отвергнутых; ADR-003 — superseded; создан ADR-004 (Proposed); golden-set.md — обязательное поле `llm_model`; CLAUDE.md — модель в строке стека.

## Сделано 2026-07-07
- Решение CEO: живой custdev Фазы 0 закрыт — единственный целевой пользователь = сам CEO (BR-4 teacher-only), ценность подтверждена напрямую. Вторая половина гейта (прогон рубрики FR-5 на чужих реальных репо) остаётся.
- **Б1 закрыт (решение CEO):** канонический ключ вердикта FR-5 — четвёрка хешей; FK на снапшоты — провенанс; отметка FR-10 переживает реверт. Внесено в `data-model.md` (§1.10, §1.12, И3).
- **Б2 закрыт:** `SyncRunRepository` (§1.13, И11) + `lesson.date`; заодно закрыт С5 (маппинг исходов на UI — в ADR-002).
- **В10 закрыт:** PRD → v2.4, §7 исправлен («адрес репозитория — не ПДн»).
- CSV реестра получен от CEO (4 студента из ~25!): С-01 — «частично», сильнейшая работа (модель как DATABASE.md без ER/трассировки; main пуст, MR не смержен; прочитана через glab-токен v.genarov); С-02 — «частично» (нет data-model.md, PRD/JTBD добротные, битая трассировка); С-03 и С-04 — репо недоступны. Сводка: `digital-twin/staging/проверка-репозиториев-студентов-2026-07-07.md`.
- Задание занятия 7 срежиссировано: `course/занятие-07-архитектура-стек-llm-задание.md` (4 шага, деливераблы-контракты, шпаргалка промптов, заметки преподавателю).

## Сделано 2026-07-02
- Решения CEO: рёбра = полный конвейер до кода/тестов; «частично» = неточное имя или заглушка; деплой допустим при максимуме данных у преподавателя; удаление ПД через 30 дней после защиты.
- PRD v2.2: переписан самодостаточно (глоссарий §0, пример ядра §5.1); проверен агентом-«посторонним читателем» с чистым контекстом — вердикт «понятен с оговорками», все 8 находок починены (CEO, разделение канона US↔PRD, связь Г↔A, статус прогона ядра vs гейт, потоки/хостинги, источник шаблона, шкала уверенности, пример рубрики).
- Рассинхрон US-B3↔FR-6 починен: слепая зона = невидимость репозитория; тишина = «хроники» (US-B4/FR-7).
- Схема данных: адверсарное ревью 8 сценариями (`reviews/ревью-схемы-данных-2026-07-02.md`) → итоговый `data-model.md` (словарь draft 2 + логическая модель + находки ревью); черновики в `archive/`; структура Override принята «2 FK + XOR»; выявлен рассинхрон PRD §7 по ПДн (В10 — требует правки PRD).

## Выполненные тикеты (первая итерация)

| Тикет | Задача | Статус | Ветка |
|-------|--------|--------|-------|
| #1 | S3 — Миграции + DDL-инварианты | ✅ закрыт | main |
| #2 | I1 — Скелет приложения | ✅ закрыт | main |
| #3 | S2 — store.py | ✅ закрыт | main |
| #4 | G1 — git_client | ✅ закрыт | main |
| #5 | S1 — Модели данных (11 ORM-сущностей) | ✅ закрыт | main |
| #6 | S4 — config.yaml + config_manager | ⏳ полигон worktree занятия 12 | — |
| #7 | S5 — Аутентификация | ✅ закрыт | main |
| #8 | S6 — CSV-импорт репозиториев | ✅ закрыт | main |
| #9 | G2 — sync_orchestrator | ⏳ полигон worktree занятия 12 | — |
| #12 | D1 — matrix_builder (12 тестов, test-first на эфире 11) | 🔧 PR на merge CEO | `D1-matrix-builder` |
| #15 | O1 — Override store (data layer) | ✅ закрыт (PR #21) | main |

## Ближайшие шаги
- **CEO: force-push переписанной истории** (команда — в итоге сессии 2026-07-23), затем merge PR веток `D1-matrix-builder` и `docs-2026-07-23`.
- Мини-эвал `deepseek-v4-flash` агентами вне эфира (решение CEO 2026-07-23): golden set + 4–6 пар из реальных репо (С-01, С-02) → ADR-004 Accepted + прогон рубрики Фазы 0. Блокер: нужен рабочий ключ DeepSeek API от CEO.
- Репетиция блока 4 занятия 12: два worktree + два opencode на #6/#9, полный цикл до слияния (чек-лист «До эфира» в файле занятия).
- Перед эфиром: pip-audit втихую, pytest 48 passed, ruff чистый.
